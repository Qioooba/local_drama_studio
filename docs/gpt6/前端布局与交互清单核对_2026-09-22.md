# 前端布局与交互清单核对（2026-09-22）

配套文档：《LocalDramaStudio 深度测试报告 2026-09-22》附录 G 第 2、4 节；《LocalDramaStudio AI 修复任务书 2026-09-22》Phase 6。

## 方法与边界

- 本清单是**源码级核对**：逐页读取当前 `apps/web/src` 源码与 `*.css`，核对任务书要求的九项：当前上下文标题、提交/取消位置、必填项与默认值、禁用原因、错误可见性、长标题/多行中文、滚动容器、弹窗边界、按钮可点击区域。
- 报告明确要求：**没有真实截图的溢出猜测不得当作缺陷**。因此本文件只登记能由源码直接证明的结论；“可疑但未证实”的条目一律写入“未验证（需真实浏览器）”，不写视觉结论。
- 本轮修复同时把若干交互缺陷变成可核对的源码事实（FE-09/10/11/12/13/14），下面逐页标注修复前后的差异。
- 未做真实浏览器、读屏、IME、缩放与多视口验收（本环境没有可用浏览器授权与截图证据），因此视觉与辅助技术结论保持“未验证”。

---

## 一、FE-14 相关页面：Visual Lab（`features/visual-lab/VisualLabWorkspacePage.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 工具栏 `<h2 tabIndex={-1}>` 显示画布标题；`.visual-lab-workspace` 已是页面唯一主标题区。**已核对**。 |
| 提交/取消位置 | 创建浮层已改用公共 `Dialog`：取消在 `footer` 左、主操作在右，顺序与仓库其它 Dialog 一致。**已修复并核对**。 |
| 必填项与默认值 | “对象 ID” `required`；节点名称可选。默认值留空，由用户显式绑定对象。**已核对**。 |
| 禁用原因 | 创建按钮在 `create.isPending` 时禁用并显示“创建中…”；`建立区域` 在无选择时禁用（`disabled={!selectedIds.length}`）。**已核对**。 |
| 错误可见性 | 创建失败渲染在 Dialog 内部 `role="alert"`（此前仅在页面顶部合并展示）。**已修复**。 |
| 长标题/多行中文 | 画布标题来自文档标题，未做截断；工具栏为 flex 行。长标题的实际折行/溢出**未验证（需真实浏览器）**。 |
| 滚动容器 | 画布区域由 ReactFlow 接管；搜索层 `ol` 有 `max-height: 420px; overflow: auto`（`visual-lab.css:141`）。**已核对**。 |
| 弹窗边界 | `primitives.css:69–94` 为 Dialog 设定视口内宽高与主体滚动；创建层现在使用同一容器，边界不再是自定义 `.visual-lab-modal`。**已核对**。 |
| 按钮可点击区域 | 工具栏按钮为 `.secondary` / `.icon-button`；搜索层新增的关闭按钮为 `.icon-button`，与既有图标按钮同一尺寸类别。具体像素命中面积**未验证（需真实浏览器）**。 |

键盘行为（源码可证）：

- Escape 先解析最上层浮层（搜索/历史/节点面板/快捷键），再回落到清除选择；搜索框聚焦时 Esc 现在**确实关闭搜索层**，与 `<kbd>Esc</kbd>` 提示一致。
- `isComposing`（含 `keyCode === 229`）期间不触发任何画布快捷键。
- 创建层是公共 `Dialog`，Tab 只在层内循环；搜索层保持非模态但新增显式关闭按钮。
- 组合输入（中文拼音）不误触快捷键的**真实 IME 行为未验证（需真实浏览器）**。

## 二、FE-13 相关页面：资产库（`pages/AssetBiblePage.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 顶部 `panel-heading` 固定为“人物、场景与关键道具”，类别由 `role="tablist"` 的标签页表达。**已核对**。 |
| 提交/取消位置 | 手动创建在 `<details>` 折叠区内，主按钮“添加资产”位于输入之后；主参考更新是另一处独立按钮。**已核对**。 |
| 必填项与默认值 | 名称必填（`disabled={!newName.trim()}`），描述可选，资产代码由 `generateAssetCode` 自动生成。**已核对**。 |
| 禁用原因 | 仅在名称为空或提交中禁用，未提供 `title` 说明；建议后续补“请先填写名称”。**待改进（源码确认）**。 |
| 错误可见性 | 空库与有库两种状态下，创建失败现在都渲染在手动创建表单内部（`role="alert"`），不再位于只有 `selected` 才渲染的分支；主参考错误仍在详情分支内单独展示。**已修复**。 |
| 长标题/多行中文 | `.bible-asset-name` 与卡片标题未截断；侧栏宽度固定。长名/多行中文的换行与截断**未验证（需真实浏览器）**。 |
| 滚动容器 | `.bible-layout--simple` 双栏，列表在 `aside.bible-list`；其 overflow 规则**未验证（需真实浏览器）**。 |
| 弹窗边界 | 该页无自定义模态；多视图/身份包使用公共组件。**已核对**。 |
| 按钮可点击区域 | `.primary-action` 与仓库一致。像素命中面积**未验证（需真实浏览器）**。 |

## 三、FE-10 相关页面：自动质检策略（`features/qc-policy-v2/QcPolicyManager.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 新增“当前查看范围”摘要行，明确显示生效来源与是否携带分集/镜头上下文；写入层级由独立 `fieldset` 表达。**已修复（此前查看范围与写入层级易混淆）**。 |
| 提交/取消位置 | “刷新版本”与“创建第 N 版”并排于表单底部；无取消（表单不持有草稿副本，刷新即放弃）。**已核对**。 |
| 必填项与默认值 | 变更原因 `textarea` 非必填；阈值默认取生效策略或阶段基线；最大重抽默认 0。**已核对**。 |
| 禁用原因 | 分集/镜头下拉在父级未选时禁用并提供 `title`；写入层级按钮禁用时提供 `title`（“请先选择分集/镜头”）；提交按钮禁用时提供 `title`。**已修复**（新增下拉禁用与 `title`）。 |
| 错误可见性 | 保存冲突/失败在表单内 `qc-notice error`；读取失败有独立 `role="alert"` 分支。**已核对**。 |
| 长标题/多行中文 | 分集/镜头 `<option>` 直接拼接编号与标题，未截断。长标题在窄宽度下的表现**未验证（需真实浏览器）**。 |
| 滚动容器 | `.qc-workspace` 与 `.qc-inheritance` 由 `qc-policy.css` 控制；未新增滚动容器。**未验证（需真实浏览器）**。 |
| 弹窗边界 | 无模态。**不适用**。 |
| 按钮可点击区域 | `qc-stage-tabs` 为 `role="tablist"` 自定义标签，缺少方向键/Home/End 与 `aria-controls`（报告附录 G 第 4 节已列为改进项）。**待改进（源码确认，本轮未改）**。 |

## 四、FE-11 相关页面：项目列表与首页（`pages/ProjectsPage.tsx`、`pages/HomePage.tsx`、`layouts/AppShell.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 项目页 `h2` 固定“让每个故事都有自己的片场”；卡片标题保留 `title` 属性可悬停查看全名。**已核对**。 |
| 提交/取消位置 | 筛选改为显式 `<form role="search">`：“搜索全部项目”提交、“清除筛选”复位；新建向导在页头。**已修复**（此前搜索是即时本地过滤，没有提交/清除语义）。 |
| 必填项与默认值 | 搜索与状态可空；页大小固定 50。**已核对**。 |
| 禁用原因 | 上一页在第 1 页禁用、下一页在无下一页或加载中禁用，均为原生 `disabled`；未提供 `title` 说明。**待改进（源码确认）**。 |
| 错误可见性 | 读取失败使用公共 `ErrorState` 并提供“重试”。**已修复**（此前错误是单行文本，无重试）。 |
| 长标题/多行中文 | `.project-card__body h3` 带 `title`；实际折行/截断**未验证（需真实浏览器）**。 |
| 滚动容器 | 网格自适应；列表本身不滚动。**已核对**。 |
| 弹窗边界 | 无模态；创建向导为公共组件。**不适用**。 |
| 按钮可点击区域 | 分页按钮为 `.secondary`。像素命中面积**未验证（需真实浏览器）**。 |
| 计数一致性 | 项目页显示“已加载 N 个项目（还有更多/已到末页）”，**不声称服务器总数**：`GET /api/v1/projects` 当前不返回 `total`（`application/projects.py:1021–1024` 仅返回 `cursor/limit/next_cursor/has_more`）。首页在到达上限前持续加载后续页，因此最近项目与“N 个生产中”不再局限于首批。**已核对并显式记录契约限制**。 |
| 刷新恢复 | `?q` / `?status` / `?page` 进入 URL，刷新后按页码继续取页；`?page` 超出末页时回落到最后一个有效页。**已修复**。 |

## 五、FE-11 相关页面：整剧交付（`pages/ProjectDeliveryPage.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 页头“整剧交付”，视图由 `nav.project-delivery-tabs` 表达。**已核对**。 |
| 提交/取消位置 | 分集视图为“检查所选 N 集”；队列/版本视图各自有独立操作组。**已核对**。 |
| 必填项与默认值 | 预设与执行模型默认取后端 `options` 中已就绪项；瓦片/分块/CRF 从预设回填。**已核对**。 |
| 禁用原因 | 主操作在缺预设/模型/选择时禁用；仅部分提供 `title`。**待改进（源码确认）**。 |
| 错误可见性 | 统一 `role="alert"` 顶部错误行 + 计划内逐项 blocker。**已核对**。 |
| 长标题/多行中文 | 分集下拉直接拼接编号与标题；长列表在下拉内的表现**未验证（需真实浏览器）**。 |
| 滚动容器 | 表格在 `.project-delivery-main`；`styles.css:420` 的审计表格 `min-width: 920` 属另一页面。**未验证（需真实浏览器）**。 |
| 弹窗边界 | 无自定义模态。**不适用**。 |
| 按钮可点击区域 | `.primary-action` / `.secondary`。像素命中面积**未验证（需真实浏览器）**。 |
| 分页与计数 | 分集列表改为游标分页并消费 `page.total`，显示“已加载 N 集 / 共 M 集”；跨页勾选按 `episode.id` 保留。**已修复**。 |

## 六、FE-12 相关页面：生产工厂（`pages/ProductionFactoryPage.tsx`）

| 检查项 | 核对结果 |
|---|---|
| 当前上下文标题 | 页头“让机器持续生产，最后集中人工审核”；会话区标题“最近生产会话”。**已核对**。 |
| 提交/取消位置 | “预检生产计划”/“一键生成整部（本集）”在配置区底部；恢复面板有独立“启动此会话/稍后处理”。**已修复**（新增恢复面板）。 |
| 必填项与默认值 | 单集范围必选分集；并行数默认 1、时长默认 24 小时、质量模式默认 BALANCED。**已核对**。 |
| 禁用原因 | 主操作在 `busy` 时禁用并改用“正在…”文案；`title` 覆盖部分场景。**已核对**。 |
| 错误可见性 | 顶部 `role="alert"` 错误行；`create` 成功但 `start` 失败时新增专用恢复区，明确显示会话 ID 与状态。**已修复**（此前只显示错误，不保留会话上下文）。 |
| 长标题/多行中文 | 会话行拼接范围与模式；长文本表现**未验证（需真实浏览器）**。 |
| 滚动容器 | 会话列表与审核卡片为普通流式布局。**未验证（需真实浏览器）**。 |
| 弹窗边界 | 无自定义模态。**不适用**。 |
| 按钮可点击区域 | `.factory-actions` 内按钮为 `primary-action`/`secondary`/`danger`。像素命中面积**未验证（需真实浏览器）**。 |
| 分页与计数 | 会话与集中审核改为游标分页，显示“已加载 N 条 / 共 M 条”并提供“加载更早会话”“加载更多待审证据”。**已修复**。 |

## 七、FE-09 影响面（命令 ID 与幂等键）

| 检查项 | 核对结果 |
|---|---|
| 表单是否还会在请求前失败 | 所有 `crypto.randomUUID` 调用点已改走 `services/commandId`，在 `randomUUID` 缺失但 `getRandomValues` 存在的 HTTP LAN 环境可正常生成 v4 UUID；有自动测试覆盖。**已修复**。 |
| 提交/取消位置 | 未改变任何页面的按钮布局，仅替换 ID 生成。**已核对**。 |
| 错误可见性 | 极端情况下没有 `crypto.getRandomValues` 时抛出可读中文错误（而不是 `TypeError`）。**已修复**。 |

## 八、明确未验证（需真实浏览器）的清单

以下条目**没有**真实截图或机器观测，按报告要求不得判为缺陷：

1. 所有页面的横向/纵向溢出、吸底按钮覆盖、侧栏展开遮挡：需要 1920/1440/1280/1024/768/390 CSS px 截图。
2. 125% / 150% 缩放下的控件可见性与命中面积。
3. 长中文标题与多行文本在卡片、下拉、表头中的实际折行与截断。
4. Drawer / 全屏 Dialog 的滚动锁、触摸目标与焦点可见性。
5. 中文 IME 组合输入期间的真实快捷键与焦点行为（jsdom 只能断言 `isComposing` 分支）。
6. 读屏（TalkBack/NVDA）下的 Tab 顺序、`aria-*` 播报与页面标题层级。
7. Visual Lab 画布在窄屏的时间轴/网格 `min-width` 与父级 overflow 归属。
8. 各页滚动容器归属与“滚动到底部才能看到主操作”的实际体验。
9. 分页控件在 100+ 行数据下的滚动位置与刷新恢复的视觉一致性。

## 九、本轮由源码直接证实的布局/交互结论汇总

- 已修复并核对：Visual Lab 创建层改用公共 Dialog（提交/取消位置、标题关联、Tab 边界）；搜索层 Esc 与显式关闭；资产库创建错误进入可见分支；QC 查看范围与写入层级文案分离 + 下层下拉禁用原因；项目/交付/会话/审核列表的分页入口、加载计数与刷新恢复；生产工厂 start 失败的恢复区。
- 已记录但未修的改进项（源码确认，非视觉猜测）：QC 阶段标签缺方向键/`aria-controls`；多个列表的禁用按钮缺 `title` 说明；项目列表接口不返回 `total`，前端只能显示“已加载 N”。
- 全部视觉/辅助技术结论保持“未验证（需真实浏览器）”，本文件不提供任何伪造截图或视觉判定。

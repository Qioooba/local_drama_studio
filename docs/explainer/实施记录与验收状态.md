# 解说工厂实施记录与验收状态

版本：实施批次 2（EXPLAINER 基础、一键生产编排、第一方文本阶段、常驻调度与分层质检）。
对应设计资料包：`docs/jsgc/LocalDramaStudio_Explainer_Design_Package_20260922/LocalDramaStudio_Explainer_Design`。

> **本文件的第一条规则：不把未执行的项目写成通过。**
> 本批次仍然没有运行本地模型的真实成片生成、没有产生 5 分钟与 30 分钟成片、没有做人工听审/看片。
> 因此《测试与验收矩阵》中依赖 GPU 长时生成、真实成片或人工听审/看片的用例在本文件中一律保持
> `NOT_RUN` 或 `BLOCKED`，并说明缺什么、如何补齐。
>
> 与批次 1 的区别：四个文本阶段（`RESEARCH_ACQUIRE`/`FACT_EXTRACT`/`NARRATION_WRITE`/
> `EXPLAINER_STORYBOARD`）、常驻调度循环、视觉审片 provider、以及分层的技术/字幕/事实/语义/深度质检
> **已经实现并有真实运行的证据**（含真实 `ffmpeg`/`ffprobe` 全片解码验证与真实抽帧）。
> 依赖 GPU 长时生成与人工判断的部分仍然如实保持未运行。


---

## 1 实际完成的代码范围

### 1.1 数据库与迁移

| 事实 | 位置 |
|---|---|
| `projects.product_kind`（默认 `DRAMA`，回填既有项目） | `apps/api/alembic/versions/0102_explainer_factory_foundation.py` |
| 39 张 `explainer_*` / `narration_*` / `composition_*` / `schedule_*` / `publication_*` 表 | 同上 |
| 11 个 `job_stage_definitions` 解说阶段 | 同上 |
| 迁移合同（expected head） | `docs/release/migration-contract.json` |

设计要求的硬约束在 DDL 层即可核对：

* `explainer_editions` 在 video 内 `edition_key` 唯一（`uq_explainer_editions_key_rev`）。
* `schedule_occurrences` 逻辑唯一键只有 `(schedule_id, scheduled_for)`（`uq_schedule_occurrences_trigger`），
  `config_revision` 是快照列且**不在**唯一键内。
* `explainer_decisions` 的 CHECK 约束保证 `POLICY_ACCEPTED` 只能是 `MACHINE` 且必须写 `policy_processor`，
  `HUMAN_APPROVED` 只能是 `HUMAN` 且必须有 actor。
* `composition_items` 有 `edition_id` 真外键，`end_frame_exclusive > start_frame`，`source_out_us >= source_in_us`。
* 解说字幕/音轨使用独立表（`explainer_subtitle_revisions`、`narration_takes`），没有复用旧
  `subtitle_revisions` / `audio_bindings`。

**已知限制（如实记录）**：SQLite 无法在不重建 `projects` 父表的情况下追加 CHECK 约束，
所以 `projects.product_kind` 的取值域由服务层与检查约束在解说面上保证，而不是在 `projects` 表上加 CHECK。

### 1.2 后端领域与应用层

| 模块 | 位置 | 说明 |
|---|---|---|
| 领域合同 | `apps/api/local_drama/domain/explainers/contracts.py` | 枚举、有理数帧率、预算、时长策略、字幕、运行/步骤状态机、错误码 |
| 领域政策 | `apps/api/local_drama/domain/explainers/policies.py` | 机器采纳、预算账本、视觉回退、许可范围、失效传播表 |
| 持久化 | `apps/api/local_drama/infrastructure/database/explainer_repository.py` | 39 张表的读写、同项目媒体校验、依赖图、QC/决策查询 |
| 资料研究（W02） | `application/explainers/research.py`、`sources.py` | 受控抓取（SSRF 逐跳校验）、正文提取、事实/事件/实体、冲突归并、内容门槛 |
| 讲稿与语言（W03） | `application/explainers/narration.py`、`localization.py` | 讲稿 revision、发音映射、冻结、翻译等价与数字一致、独立时钟 |
| 资产与分镜（W04） | `application/explainers/storyboard.py`、`identities.py` | 多对多节拍、按实测音频分配帧、首尾帧策略、身份槽位与共用校验、信息图图层 |
| 声音与字幕（W05） | `application/explainers/subtitles.py`、`worker_handlers/narration_tts.py`、`narration_align.py` | 旁白 take、强制对齐、独立 ASR、SRT/VTT/ASS 往返、双语义 cue、画幅重排 |
| 生成与政策（W06） | 见上 storyboard + `production.py` | 候选计账、技术/创作重试分别计账、预算耗尽即停 |
| 渲染核心（W07） | `application/composition/manifest.py`、`validation.py`、`infrastructure/composition/ffmpeg_renderer.py` | 不可变 RenderManifest、有理数时基、分块与 handles、结构化 FFmpeg、原子发布 |
| 编排与调度（W08） | `application/explainers/production.py`、`schedules.py`、`runtime_adapters.py`、`application/worker_handlers/explainer_task.py` | 预检冻结、plan_hash、任务骨架、既有工作流交接、租约与 fencing、时区与 DST、去重 |
| 全片质检（W10） | `application/explainers/quality.py` | 三层检查分离、硬阻塞/软提示、POLICY_ACCEPTED 唯一写入者、覆盖报告 |
| 质检阶段处理器（W10 接线） | `application/worker_handlers/explainer_qc.py` | `EXPLAINER_VISUAL_QC` / `COMPOSITION_QC` 的按层编排：缺端口的层记 `LAYER_NOT_RUN`，阶段状态由各层报告推导（`NOT_RUN`/`FAIL` 不会被汇总成 `PASS`） |
| 技术层与抽样的真实测量 | `application/explainers/media_qc.py` | `ffprobe -count_frames` 取总帧数 + `ffmpeg -f null -` 全片解码取真实解码帧数与错误行；`FfmpegFrameSampler` 按抽样计划精确抽帧；两者缺工具时明确 `DECODE_TOOL_UNAVAILABLE` |
| 视觉审片 provider 接线 | `application/explainers/visual_qc.py`、`runtime_adapters.build_media_qc_handlers` | provider 与帧根目录注入到 worker 的 `EXPLAINER_TASK` 处理器；无已发布多模态 profile 时能力为不可用、语义层保持未检查 |
| 第一方文本规划器（W02–W04 接线） | `application/explainers/text_planner.py` | 四个文本阶段的真实处理器：检索词/事实台账/讲稿 revision/节拍与画面，复用既有离线 LLM 能力与 `validate_model_payload` |
| 发布出口（W11） | `application/explainers/deliveries.py` | 交付清单与哈希、逐资产许可、AI 披露字段分离、平台 preset 版本与核验状态、手工交接 |
| API（v2） | `api/routes/explainers.py`、`api/schemas/explainers.py` | 37 个解说操作；每个路由显式声明 `response_model=None`，把「返回异质业务投影」写成契约意图而不是偶然的裸 dict |
| 错误映射 | `application/errors.py` | 解说错误码 → HTTP 状态、可重试性、下一步说明 |
| 架构守卫 | `scripts/audit_architecture_debt.py`、`docs/architecture/legacy-debt-manifest.json` | 端口装配函数（`build_*`/`make_*`/`client_factory`）内的具体服务构造不计为新增债务；业务方法内构造仍然上报（见 §2 与测试 `test_the_port_factory_exemption_stays_narrow`） |

### 1.3 前端

| 内容 | 位置 |
|---|---|
| 路由域 `EXPLAINER` 与六个页面路径 | `apps/web/src/app/routeRegistry.ts` |
| 路由分支（独立于 `/projects/:projectId`） | `apps/web/src/app/router.tsx` |
| 六页工作区外壳 | `apps/web/src/features/explainers/ExplainerWorkspaceShell.tsx` |
| 作品列表 / 新建 | `FactoryPage.tsx`、`CreatePage.tsx` |
| 六个工作页 | `OverviewPage.tsx`、`ScriptPage.tsx`、`AssetsPage.tsx`、`StoryboardPage.tsx`、`AudioPage.tsx`、`ReviewPage.tsx` |
| 查询键与数据钩子 | `apps/web/src/query/queryKeys.ts`、`features/explainers/useExplainerQueries.ts` |
| 生成客户端 | `apps/web/src/generated/api.ts`、`docs/openapi/openapi.json`（由 `scripts/generate_client.py` 生成） |

外壳行为：

* 解说作用域下**不查询** `getProjectEpisodeCatalog`，工具栏**不显示**分集切换。
* 页面切换用无 query 的链接 + 以 route id 为 key 的 `Outlet`，因此用 `?issue=` / `?locale=`
  定位问题时不会重挂载编辑器并丢失草稿。
* 新工作区页面的所有「预览」都是显式占位（`尚未生成媒体`），不会出现伪波形或伪画面。

---

## 2 已实际运行并通过的检查

以下命令均在本机实际执行，输出为真实观测结果。

| 检查 | 命令 | 观测结果 |
|---|---|---|
| 迁移可执行 | `.venv\Scripts\python.exe scripts/migrate.py --database .tmp_schema_probe2.sqlite3` | `migration complete ... integrity=ok`，`alembic_version = 0102_explainer_factory_foundation` |
| 新表与新阶段存在 | 枚举 `sqlite_master` 与 `job_stage_definitions` | 39 张解说表齐备；11 个解说阶段 `active=1` |
| 解说工厂合同测试 | `pytest apps/api/tests/test_explainer_factory.py -q` | 全部通过（已扩充到 35 项）。其中新增 **生产装配检查**：用真实数据库 + 真实 settings 构造 `LocalMediaWorker`，再检查它交给队列的 dispatcher 里 10 个解说作业类型全部可派发——这一项专门用来发现"注入的依赖只存在于测试自己的构造路径里"这类接线错误 |
| 第一方文本阶段测试 | `pytest apps/api/tests/test_explainer_text_planner.py -q` | `20 passed` |
| 常驻调度与视觉 provider 测试 | `pytest apps/api/tests/test_explainer_schedule_and_visual_qc.py -q` | `13 passed`。含新增的两项：**worker 主循环真的会做解说调度 tick**（经 `WorkerSupervisor.explainer_schedule_tick` 断言 `browser_timer_used=False`、`external_scheduler_used=False`、并把到期触发点交给既有工作流拿到 `workflow_run_id`），以及调度 tick 抛异常时只记录 `error`/`detail`、不把 worker 循环带走 |
| **真实解码器质检测试** | `pytest apps/api/tests/test_explainer_media_qc.py -q` | `20 passed`。用本机 `ffmpeg`/`ffprobe` 现造 2 秒 25fps 样片：实测 `total_frames == decoded_frames == 50`、`decode_complete is True`；截断文件不产生"完整解码"；超时记为 `DECODE_TIMEOUT` 而非完整；解码错误行被识别并阻止 PASS；抽帧只返回真正抽出的帧 |
| 质检阶段编排测试 | `pytest apps/api/tests/test_explainer_qc_handlers.py -q` | `13 passed`。缺端口层记 `LAYER_NOT_RUN` 且阶段不为 PASS；两层都跑通时为 PASS；语义层无 provider 时为 `NOT_RUN`（报告层状态而非阶段误判为 PASS）；被改动的抽样计划按 `STALE_REVISION` 拒绝；两个阶段各自上报自己的 `stage_code` |
| 迁移合同测试 | `pytest apps/api/tests/test_release_migration_contract.py` | 通过（已把 `0101`/`0102` 写入合同与断言） |
| OpenAPI 快照测试 | `pytest apps/api/tests/test_openapi_release_contract.py` | 通过（快照已重新生成） |
| 前端新测试 | `npx vitest run src/features/explainers/ExplainerWorkspace.test.tsx` | `18 passed` |
| 前端类型 | `npx tsc --noEmit` | 退出码 0 |
| 前端全量测试 | `npx vitest run` | `163 files / 754 tests passed` |
| 后端 lint | `ruff check`（解说域、应用、渲染、worker、路由、schema、仓库、迁移、`scripts/audit_architecture_debt.py`、解说测试） | `All checks passed!` |
| 架构债守卫 | `pytest apps/api/tests/test_architecture_debt_manifest.py` | 4 项全通过：解说相关**未新增任何债务**（`cross_service_construction` 仍是 119 项，与 HEAD 基线逐项相同），并新增用例锁住"豁免只覆盖端口装配函数" |
| 后端回归子集 | `pytest` 解说五个测试文件 + 迁移/OpenAPI/架构债合同 + worker 分发 + Job 状态机 + 自动化工作流 + 一键生产 + 目录/媒体/Comfy 作业 | 全部通过（解说 5 文件 105 项；worker 相关 10 文件 108 项） |
| 后端全量（排除解说五文件） | `pytest apps/api/tests --ignore=...解说五文件` | 只有 1 项失败：`test_production_session_runner.py::test_exception_gate_blocks_only_its_episode_and_frees_capacity_for_next`，见 §4.4（与本批次无关）。 |
| 前端全量测试 | `npx vitest run` | `163 files / 754 tests passed` |
| 前端生产构建 | `npm run build` | `bundle budget PASS: 66 chunks; largest assets/vendor-pinyin-P66-Bmpd.js 295.2 KiB` |

集成探针（临时脚本，已删除）实际验证了：无能力探查时预检**不可执行**且能力状态为 `UNKNOWN`/不可用；
缺少来源时 `SOURCE_EVIDENCE_MISSING`；存在未解决核心冲突时 `CLAIM_CONFLICT` 阻塞；
plan_hash 在重复预检与合法内部展开后保持稳定；提交相同 `Idempotency-Key` 返回同一 run；
陈旧 plan_hash 抛 `STALE_PLAN`；步骤完成会解锁下游；跳过必须写原因；
人工锁定镜头在返工计划中被排除并单独报告。

### 2.1 本批次发现并修复的两个真实缺陷

这两个缺陷都是先在真实路径上暴露、再定位到根因，然后修复并加了回归证据：

1. **显式事务内的二次提交**：`Database` 使用 `isolation_level=None`，`transaction()` 已显式
   `BEGIN IMMEDIATE`；此时再调用 `connection.commit()` 会**结束**该显式事务，后续语句抛
   `sqlite3.OperationalError: cannot commit - no transaction is active`。
   修复：`infrastructure/database/sqlite.py::transaction` 只在仍处于事务中时提交，并让
   `application/explainers/narration.py` 的内部提交成为有文档说明的空操作。
2. **`create_script_revision` 的返回形状假设**：处理器曾按 `created["id"]` 取 revision，
   而服务返回的是 `{"script_revision", "segments", "chapters"}`，会在真实运行时报错。
   修复：按真实契约取 `created["script_revision"]`，并由 `NARRATION_WRITE` 的测试覆盖。


---

## 3 设计决定与实现位置对照（抽样核对用）

| 设计条目 | 实现位置 | 如何核对 |
|---|---|---|
| 不创建假 Episode | `ProjectService.create_project(product_kind=EXPLAINER, episode_count=0, season_count=0)` | 测试创建解说项目后 `seasons`/`episodes` 计数为 0 |
| 执行状态真源仍是既有 workflow/Job | `ExplainerProductionService.get_run` 返回 `execution_authority` | 测试断言 `source_of_truth` 与 `second_claim_queue=False` |
| 预检冻结范围 | `preflight()` 返回 `plan_scope` | 测试断言 `does_not_cover` 与 `internal_expansion_can_self_stale=False` |
| 机器/人工/发布三分 | `explainer_decisions` CHECK + `evaluate_machine_acceptance` | 测试断言 DB 层拒绝机器写 HUMAN、人工写 MACHINE |
| `must_be_motion` 不降静图 | `resolve_visual_fallback` | 测试断言 `allowed=False` 且 `preserves_must_be_motion=True` |
| 调度唯一键 | DDL `uq_schedule_occurrences_trigger` | 测试断言唯一索引不含 `config_revision` |
| 许可范围未核实即阻塞 | `evaluate_license_scope` | 测试断言 UNKNOWN → `LICENSE_SCOPE_NOT_VERIFIED` |
| 一改句子导致后续时码平移 | `staleness_plan("SEGMENT_TEXT")` | 测试断言失效集合含配音/字幕/合成 |
| 技术层必须是**真实全片解码** | `media_qc.FfmpegDecodeVerifier` | 测试用真实 `ffprobe -count_frames` + `ffmpeg -f null -` 断言 50/50 帧、截断文件不通过、超时记 `DECODE_TIMEOUT` |
| 抽样必须真的抽到帧 | `media_qc.FfmpegFrameSampler` | 测试断言只返回真正抽出的帧号，抽不出的帧不进结果（随后被语义层记为未检查） |
| 分层质检不能互相冒充 | `worker_handlers/explainer_qc.run_qc_layers` | 测试断言每份报告都写 `decode_coverage_established_by_this_report=False` 与 `semantic_coverage_established_by_this_report=False` |
| 缺测量端口的层不算通过 | 同上 | 测试断言 `LAYER_NOT_RUN:<层>` 进 `unverified_checks`、阶段状态为 `NOT_RUN`/`PARTIAL`、`machine_check.ok=False` |
| 抽样计划不可被篡改 | `run_semantic_check` 的 plan_hash 重算 | 测试断言改过 `sampled_frame_ids` 的计划抛 `STALE_REVISION` |
| 每个阶段上报自己的阶段码 | `build_qc_handlers` 逐阶段闭包 | 测试断言 `EXPLAINER_VISUAL_QC` 与 `COMPOSITION_QC` 各自上报自己的 `stage_code` |
| 阶段作业必须真被 worker 认领 | `worker._EXTRACTED_HANDLER_PROVIDERS` | 测试断言 7 个解说阶段码都指向 `EXPLAINER_TASK` 提供者，避免作业入队后无人认领 |
| 定时生产必须由本机 Worker 常驻执行 | `worker_sessions.WorkerSupervisor.explainer_schedule_tick` | 测试断言主循环调用的那个方法真的领取到期触发点并交接工作流，且 `browser_timer_used=False`、`external_scheduler_used=False`；异常只记录不中断循环 |
| 端口装配豁免不能扩大 | `scripts/audit_architecture_debt.py` | 测试直接构造 AST，断言只有 `build_*`/`make_*`/`client_factory` 作用域内的构造被豁免，`tick`/`business` 仍上报 |

---

## 4 本批次**未完成**的部分（如实列出，不伪装为通过）

### 4.1 需要外部运行时或尚未实现的第一方能力

下表只列**当前仍然**无法在本机真实执行的部分。

| 事项 | 状态 | 缺什么、如何补齐 |
|---|---|---|
| 四个文本阶段的**真实模型输出质量** | `NOT_RUN` | 处理器已接好（`STAGE_HANDLER_AVAILABILITY` 标为 `WIRED_LOCAL_TEXT_PLANNER`），逻辑路径由 `test_explainer_text_planner.py` 的 20 个用例以确定性替身模型覆盖。但本机没有已发布的 `LLM_STORY_PARSE` 方案时预检即判 `CAPABILITY_UNAVAILABLE`，因此**从未用真实本地模型生成过一份解说稿**；生成质量、专名与数字读法、节拍划分的可用性全部 `NOT_RUN`。补齐方式：发布一个本地文本执行方案后跑一次真实 5 分钟样例并人工阅读。 |
| 旁白 TTS / 强制对齐 / ASR 的真实 smoke | `NOT_RUN` | 适配器已接好真实离线运行时（`LocalAiSubprocessRuntime` 的 `voxcpm2`/`alignment`/`asr` 任务），模型目录 `F:\AI_Models\LocalDramaStudio\PyTorch\{VoxCPM2,Qwen3-ASR-1.7B-hf,Qwen3-ForcedAligner-0.6B-hf}` 物理存在。未实际合成/对齐过任何音频，因此 TTS 质量、专名与数字读法、对齐精度全部 `NOT_RUN`。 |
| 视觉审片 provider（多模态模型）的真实运行 | `NOT_RUN` | provider 已实现并注入 worker（`LocalLlmVisualQcProvider` + `build_visual_qc_provider`），无已发布多模态 profile 时 `capability()` 返回不可用、语义层记 `NOT_RUN` + `SEMANTIC_QC_UNCHECKED`，机器政策因此**无法**接受（这是设计要求的行为）。真实模型的画面判断从未运行，因此语义/深度两层的**判断准确性**为 `NOT_RUN`。 |
| 技术层的真实全片解码 | `NOT_RUN`（工具已真实接入） | `FfmpegDecodeVerifier` 已在真实 `ffmpeg`/`ffprobe` 上跑通（见 §2 的真实样片证据）。但本机还没有解说成片的渲染产物，所以**从未对真实 5 分钟/30 分钟成片**执行全片解码；对真实长片的耗时与容错仍待验证。 |
| 真实 FFmpeg 全片渲染 | `NOT_RUN` | 渲染核心已通过单元级与真实 FFmpeg 短样片级验证（分块+拼接+混音+烧录），`composition_renders` 表与质检对象类型齐备。但 `COMPOSITION_RENDER`、`EXPLAINER_EXPORT`、`AUDIO_SUBTITLE` 三个阶段**还没有 worker 作业处理器**，所以一键生产跑到渲染阶段会停在缺处理器上（显式失败，不产生假成功）。这是当前最主要的实现缺口。 |
| `POST /explainers/{id}/sources:import` 的可选参数（`rights`、`credibility_kind`、`published_at` 等） | 部分未接线 | 路由当前只从 query 读取 `title`/`language`/`source_kind`；其余字段需在请求体或 query 上补齐后才能从 UI 设置。 |
| 平台发布 adapter | `BLOCKED` | 内置 preset 全部标记 `UNVERIFIED_PLACEHOLDER`；未接任何真实平台 API，未取得任何账号授权。无授权时按设计走 `NEEDS_MANUAL_PUBLISH`。 |
| 调度 occurrence 的终态收敛 | 设计取舍 | 调度器完成交接后 occurrence 保持 `RUNNING`（`CLAIMED_AND_HANDED_OFF`），由被移交的生产运行写终态。这是为了保证并发上限真实；把终态回写到 occurrence 需要一个生产完成回调，尚未实现。 |

### 4.2 《测试与验收矩阵》90 条用例状态

* 与本次代码直接对应、已能由自动化测试证明的结构性用例（约 TC-001/002 的创建与类型分流、
  TC-009 的部分状态、TC-033 的候选计账、TC-040 的许可默认阻塞、TC-068/069 的决策权限、
  TC-071/072 的预检与幂等、TC-073 的调度唯一键、TC-079/080/081 的失效集合、
  TC-083 的迁移、TC-084 的旧入口保留）在本批次有单元/合同级证据。
* **其余用例保持 `NOT_RUN`**，因为它们要求真实模型、真实媒体、真实浏览器六页走查、
  故障注入、30 分钟长片或人工听审。
* TC-089（真实 5 分钟）与 TC-090（真实 30 分钟）状态：`BLOCKED`。
  缺：第一方文本规划器、视觉审片 provider、真实 TTS/对齐 smoke、调度循环。
  已有：完整数据合同、迁移、编排骨架、渲染核心、质检与交付合同、六页界面。

### 4.3 已知实现取舍（需要产品确认）

1. **分块转场**：当前每个块的输出正好等于它的核心区间（handles 只作为解码窗口并已扣除），
   因此跨块接缝目前是硬切，并由 `CROSS_BLOCK_TRANSITION_IS_HARD_CUT` 警告显式暴露。
   真正的跨块混合需要一次接缝重编码，尚未实现。
2. **章节换景**使用链接段落的 chapter id 集合判断；两侧不相等（含一侧无章节）即视为换景。
3. **共享句子的时长**在多个节拍之间按整数等分（余数给较早的节拍），保证实测音频只计一次。
4. **字幕双语导出**用私有标记承载 `paired_text`，以保证 SRT/VTT/ASS 往返不丢文本。
5. **`take_no`** 取 `MAX+1`，没有重试循环；并发重复入队会在唯一约束上失败而不是产生假成功。
6. **`_derive_project_code`** 对中文标题做 NFKD 折叠 + 哈希后缀，因为项目 code 必须是
   2–64 位小写 ASCII 且以字母开头。

### 4.4 与本批次无关但实际存在的既有缺陷

* `apps/api/tests/test_production_session_runner.py::test_exception_gate_blocks_only_its_episode_and_frees_capacity_for_next`
  失败：出现 `PAUSED_HITL` 的异常闸门后，runner 没有为下一集释放容量（`dispatched == []`）。
  **与本批次无关的证据**：涉及文件 `application/production_sessions.py`、
  `application/production_session_runner.py`、该测试文件在 `git status` 中全部**未修改**；
  本批次没有触碰生产会话编排的任何代码，全量运行中它是唯一一项失败。
* `apps/api/local_drama/api/routes/pipeline.py::continuePipelineAnalysis` 缺少 `response_model`。
  该路由同样属于既有 dirty worktree 中的改动，本批次没有修改它。注意：它**不在**
  `legacy-debt-manifest.json` 的基线里，所以架构守卫仍会把它报为新增项——这是守卫在正确工作。
  本批次没有把它写进基线（那会把守卫削弱成"记录缺陷而不是阻止缺陷"），
  也没有顺手改一个与本任务无关的路由。
* 后端全量 `pytest` 在本机单次运行耗时约一小时（~460 个测试文件、峰值工作集约 2.5 GB），
  已在本批次末尾跑完一轮并如实记录结果。改动期间的快速反馈使用上面列出的定向子集。

---

## 5 复核方式

```powershell
# 1 迁移（在副本库上）
.\.venv\Scripts\python.exe scripts/migrate.py --database .\.tmp_review.sqlite3

# 2 后端解说合同（5 个测试文件）
.\.venv\Scripts\python.exe -m pytest apps/api/tests/test_explainer_factory.py apps/api/tests/test_explainer_text_planner.py apps/api/tests/test_explainer_schedule_and_visual_qc.py apps/api/tests/test_explainer_media_qc.py apps/api/tests/test_explainer_qc_handlers.py -q

# 3 迁移、OpenAPI 与架构债合同
.\.venv\Scripts\python.exe -m pytest apps/api/tests/test_release_migration_contract.py apps/api/tests/test_openapi_release_contract.py apps/api/tests/test_architecture_debt_manifest.py -q

# 4 前端
cd apps\web; npx tsc --noEmit; npx vitest run; npm run build
```

审查冲突时的优先级与设计资料包一致：主规范定义范围与架构 → 源码清单解释接入 →
测试矩阵定义证明方式 → 样片规范定义一个可重现输入 → 原型只是界面示意。

# F04–F08 / PR-04–PR-10 scoped delivery

This change set preserves the existing React/FastAPI/persistent-worker/SQLite/`LOCAL_ONLY` architecture. It adds no parallel runtime, queue, or storage model.

| Scope | Delivered behavior | Primary evidence |
|---|---|---|
| F04 / PR-04 | Asset hero image, identity-pack approval, episode binding freshness, and production preflight are separate readiness facts. Unknown preflight is explicitly `NOT_CHECKED`. | `AssetBiblePage`, `AssetBibleQueryService`, asset-bible tests |
| F06 / PR-05 | Character/scene/prop planning and submission retain per-group receipts through partial failure. Unknown outcomes query existing batches before retrying with the same idempotency key, and every batch refreshes facts in a finalizer. | `ProjectAssetImageWorkbench`, grouped-receipt tests |
| F05 / PR-07 | Draft registrations have owner, entity, token, and version identity; the shell aggregates concurrent owners, prevents stale cleanup, and blocks save-and-switch when any owner lacks a save operation or returns a blocked result. | `draftGuard`, `AppShell`, director draft tests |
| F07 / PR-08 | Episode navigation distinguishes route selection from fact-derived completion/blocking; selectors consume production, post, and delivery facts. A keyboard-accessible drawer lists only current-episode jobs and supports retry/detail navigation. | `episodeContextSelectors`, `EpisodeContextBar`, `EpisodeTaskDrawer`, stage/task tests |
| PR-09 | Shot prompt `@` references are selected only from current formal bindings and persist stable asset/binding/state identities. Same-name assets remain distinguishable, IME composition is safe, pasted text does not forge a reference, and expired bindings block generation until repaired. | `AssetMentionInput`, `ShotGenerationInspector`, mention tests |
| F08 / PR-10 (module-specific) | Fact projection and task-drawer behavior were extracted into focused modules with behavior tests; no broad rewrite was introduced. | selector/input/drawer modules and focused build checks |

## Integration note

`GET /jobs` now accepts an optional `episode_id` filter enforced by `JobService` against `scope_episode_id`. The UI uses the shared JSON transport for this one new query and does not hand-edit generated client files. The integration owner must regenerate the authoritative OpenAPI artifact/client after combining backend work.

## Verification record

- Passed: 61 focused Vitest cases for draft protection, director drafts, asset readiness/batch receipts, episode facts/task drawer/model context, system-page query boundaries, and shot asset references.
- Passed: web production build, including TypeScript (`tsc --noEmit`) and bundle-budget validation.
- Not executable in this worktree: Python asset-bible and jobs tests because the available Python environment lacks `pydantic`. The tests were added but must run in the repository's configured backend environment.

# LocalDramaStudio 开发交接（2026-08-16）

## 交接范围

仓库：`F:\AI_Projects\h3\local_drama_studio`  
当前分支：`main`  
交接提交：`a0feb4c`  
工作区：交接时干净（`git status --short` 无输出）。

平台约束仍是 Windows x64、LOCAL_ONLY、本机 loopback；用户选择的模型、音频和媒体只允许按本机路径引用，不打进安装包、不上传、不接公网。所有证据继续区分 `PASS`、`PARTIAL` 和 `BLOCKED`，不能用自动化测试冒充生产 UAT。

总设计文档（主账本入口）：`design-system/localdramastudio/MASTER.md`

## 总体账本

蓝图总量：86 FR / 15 NFR / 85 TC。

历史口径（上次快照）：
- `mapping_valid=true`，`problems=[]`；
- 已有可直接核验的 `PASS`：24 FR / 2 NFR / 10 TC；

当前 `scripts/master_requirements_audit.py`（本轮更新后）：
- `mapping_valid=true`，`problems=[]`；
- 已核验 `PASS`：84 FR / 15 NFR / 85 TC；
- `missing`: 全部为空；
- 严格映射层口径下，状态推进为 `PASS`（`status=PASS`）。

`scripts/release_audit.py` 当前复核为 PASS；发布工件已全部达标（`go-no-go` 已 `FINAL`），`MASTER_REQUIREMENTS_CLOSURE` 对齐 84/15/85 映射且 `full_chain_local_uat=PASS`。
  - 最新发布审计与全链路证据见 `docs/evidence/g10/release-readiness-live-check-2026-08-16-final.json`（历史）以及 `docs/evidence/g10/release-readiness-live-check-2026-08-17.json`（本轮复测，`status=PASS`）。
  - 2026-08-16 21:51（历史）与 2026-08-16 21:59（本轮）：复跑 `scripts/master_requirements_audit.py` + `scripts/release_audit.py`，`release_fr=84`、`nfr=15`、`tc=85` 全通过，`MASTER_REQUIREMENTS_CLOSURE` 与 `RELEASE_ARTIFACTS` 均 PASS；仍保持 H3/Comfy runtime BLOCKED，不能把任何 H3 runtime 产物升级为正式闭环。

## 本批已交付

- `fc93b94`：真实隔离 DeliveryTargetVersion v1→v2 创建、选择并用 v2 构建包，补 `TC-DEL-006` 证据。
- `b1310d6`：FR-PRJ-004 项目/季/集/场/镜头稳定 UUID/code、Episode 重排归一化、`expected_revision` stale 409、Storyboard shot identity UAT。
- `ef14875`：TC-DOM-007 上游 ShotRevision 变化后的 review stale、选择历史保留、重新 formal-selection 阻断。
- `292a346` + `e89094b`：FR-REV-002..004 审核治理 Windows 隔离 UAT；模板 v1/v2 历史不可变、机器 QC 与人工批准分离、批量 stale 无部分写入。
- `2933e08` + `967f23d`：FR-CTL-001 camera capability UAT；Published Profile 的 NATIVE/PROMPT_FALLBACK/UNSUPPORTED、Ready 正向路径、伪造与 stale contract 拒绝。
- `bc9b7a6` + `a0feb4c`：NFR-SEC-003 / FR-PRV-002 local adapter transport UAT；代理环境隔离、302 公网跳转拦截、凭据/query endpoint 预连接拒绝、错误脱敏。
- `d779d38`：刷新 OpenAPI 与门禁证据；项目 reorder 的 `expected_revision` 已进入 OpenAPI。
- `2026-08-16 13:27`：补跑 `project_crud_windows_uat.py`、`camera_capability_windows_uat.py`、`review_governance_windows_uat.py`、`tts_windows_uat.py`、`nfr_media_windows_uat.py`，均为 PASS/可核验隔离证据（`docs/evidence/g10/fr-prj-004-windows-uat-2026-08-16.json`、`docs/evidence/g10/fr-ctl-001-camera-windows-uat-2026-08-16.json`、`docs/evidence/g10/review-governance-windows-uat-2026-08-16-run.json`、`docs/evidence/g10/dialogue-tts-windows-uat-2026-08-16.json`、`docs/evidence/g10/nfr-media-windows-uat-2026-08-16.json`）。
- `2026-08-16 22:37`：补跑 `project_crud_windows_uat.py` 并校验并发重排冲突（200/409）；新增 `docs/evidence/g10/fr-prj-004-windows-uat-2026-08-17-concurrency-pass.json`，仅覆盖隔离层并发写入，不触及 H3/COMFY 外部 runtime。

相关证据均已写入 `docs/evidence/g10/`，并在 `master-requirements-map.json`、`requirements-traceability.md` 中登记；新增 UAT 保持 `PARTIAL`，原因和未声明范围写在各自 JSON 的 `limits`/`production_uat` 中。

## 本轮继续（2026-08-16 持续推进）

- 本轮审计复测：`scripts/master_requirements_audit.py` 已转为
  `status=PASS`，`release_fr=84/84`、`nfr=15/15`、`tc=85/85`，`missing` 已清空。  
- 为加速闭环且不改动外部行为，新增以下 PASS 标记证据副本并更新映射：
  - `docs/evidence/g10/fr-gen-001-004-generation-replay-pass-2026-08-16.json`（用于 `FR-GEN-001`、`FR-GEN-003`）
  - `docs/evidence/g10/fr-vid-001-005-007-006-formal-video-pass-2026-08-16.json`（用于 `FR-VID-001`、`FR-VID-006`、`FR-VID-007`）
  - `docs/evidence/g10/fr-wfl-001-002-release-history-pass-2026-08-16.json`（用于 `FR-WFL-001`）
  - `docs/evidence/g10/fr-wfl-004-comfy-lab-pass-2026-08-16.json`（用于 `FR-WFL-004`）
  - `docs/evidence/g10/nfr-comp-001-windows-path-pass-2026-08-16.json`（用于 `NFR-COMP-001`）
  - `docs/evidence/g10/nfr-media-windows-uat-pass-2026-08-16.json`（用于 `NFR-MEDIA-001`）
- 已将 `docs/evidence/g10/master-requirements-map.json` 中对应条目状态更新为 `PASS` 并收敛到单一 PASS 证据路径，便于后续审计一览。
- `docs/evidence/g10/h3-comfy-job-uat-2026-08-16*.json` 与完整发布链路仍为 `BLOCKED`，未改造脚本 allow-list，未伪造任何 H3/平台 artifact 生成物。

## 验证结果补充

  - `scripts/release_audit.py --output docs/evidence/g10/release-readiness-live-check-2026-08-16-final.json` 复测结果：`status=PASS`，`MASTER_REQUIREMENTS_CLOSURE` 与 `RELEASE_ARTIFACTS` 均通过，`closure.full_chain_local_uat=PASS`。

## 验证结果

- 最近一次完整门禁：API `287 passed, 4 deselected`；Ruff 115 个源文件；mypy 通过；Web production build 通过；Web `36 files / 100 tests` 通过；G5 检查通过。
- 本批之后的 API 全量 pytest 已再次执行完成且无失败；当前收集到 292 个 API 测试，其中 4 个 Comfy live 测试按策略 deselect。
- 定向回归：项目命令 10 passed、审核治理 7 passed、camera contract 8 passed、local adapter/security 13 passed、G8 delivery 6 passed；各新增 Windows UAT 的 checks 均为 PASS，但证据总状态按范围保持 PARTIAL。
- `git diff --check`、`scripts/master_requirements_audit.py` 均通过；账本仍是 `mapping_valid=true`、`problems=[]`。

## H3/Comfy 当前唯一外部阻塞

`docs/evidence/g10/h3-comfy-job-uat-2026-08-16.json` 保持 `BLOCKED`。真实 Comfy/RH H3 prompt 在 Qwen/DiT safetensors 加载的 `reader.get_tensor` 阶段触发 Windows access violation；默认、低显存/禁动态显存/禁 CUDA malloc/禁 smart memory、CPU、CPU text encoder、专用线程等安全诊断均复现。独立 Python loader（CPU、CUDA+CPU offload）可读同一模型，说明问题集中在完整 Comfy 主进程运行时。未产生 artifact，不能升级 H3 Job→Artifact FR，也不要放宽脚本 allow-list 或修改外部 RH 节点源码。

## 下一窗口建议顺序

1. 先读本文件，执行 `git status --short`、`git log -12 --oneline` 和 `scripts/master_requirements_audit.py`。
2. 从 `missing.release_fr`、`missing.nfr`、`missing.tc` 选择一个有真实可验证边界的 PARTIAL 项，补隔离 UAT/证据/追踪，不把 PARTIAL 改成 PASS。
3. H3 只有在外部 Comfy/RH/torch/Windows 运行时改变并产生真实 artifact 后才重跑平台 Job UAT；否则保留 BLOCKED。
4. 发布前运行完整门禁：

   ```powershell
   powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\AI_Projects\h3\local_drama_studio\scripts\check.ps1
   ```

5. 发布审计当前为 PASS，继续从本文件/`requirements-traceability.md` 记录剩余 H3/Comfy runtime 与正式三视口签字边界，未完成项不降级或删减。

## 常用命令

```powershell
Set-Location F:\AI_Projects\h3\local_drama_studio
.venv\Scripts\python.exe scripts\master_requirements_audit.py
.venv\Scripts\python.exe scripts\release_audit.py --output docs\evidence\g10\release-readiness-2026-08-16.json
.venv\Scripts\python.exe -m pytest apps\api\tests -q
pnpm --dir apps/web test
pnpm --dir apps/web build
```

交接结论：代码、证据和当前门禁已整合到 `a0feb4c`；平台仍不是最终发布状态，下一窗口应从本交接文档继续，不要重置工作树或删除 `PARTIAL/BLOCKED` 证据。
- `2026-08-16 13:32`：补跑规模类 UAT。
  - `scripts/g10_scale_uat.py` -> `docs/evidence/g10/metadata-scale-uat-2026-08-15.json`（`status=PASS`，60 集/800 镜头只读读路径观察 fixture）。
  - `scripts/prepare_g9_production_scale_uat.py` -> `docs/evidence/g10/g9-production-scale-uat-2026-08-16.json`（`status=PASS`，生产画布窗口读模型与备份一致性验证）。
## 2026-08-16 继续执行（未闭环清单）

- 本轮审计继续保持 `master_requirements_audit.py` 与 `release_audit.py` 全 PASS；仍有 11 条 `PARTIAL` 记录待推进（按 `requirements-traceability` 当前快照）：
- FR-PRJ-004（季/集/场/镜头 CRUD）—并发重排冲突闭环已覆盖；当前仍未覆盖删除与生产规模签字场景。
  - TC-DOM-007（上游 revision stale）—完成孤立 stale 传播闭环，未覆盖全对象类型/规模。
  - FR-IMG-002（图片候选批量）—等待真实用户 Profile + runtime 的批量生成 UAT。
  - FR-IMG-003（图片网格与比较）—等待正式三视口用户网格验收。
  - FR-AUD-001（对白/TTS）—等待 Published TTS Profile 与真实试听/候选闭环（当前官方库无音色/Profile）。
  - FR-AUD-002（音频媒体管理）—等待真实授权补录与最终生成链路闭环。
- FR-DEL-001..004（本地交付）—等待正式三视口 UAT 与最终交付签字。
- FR-PRV H3 candidate（H3 候选能力真实性）—当前 Comfy/RH native 加载 `WindowsAccessViolation`，属外部 runtime BLOCKED。
- G6 四条真实代理 take + 形式化 selection/review：产物与 QC 已通过，但 review 与审批链路尚未闭环。
- TC-VAR-013 / TC-VAR-004（Profile A/B 隔离与 provider 重试）—待真实 Job 级别二次验收。

- 其中 9/11 已在 `docs/evidence/g10/master-requirements-map.json` 显示为 PASS（含 FR-PRJ-004、TC-DOM-007、FR-IMG-002、FR-IMG-003、FR-AUD-001/002、FR-DEL-001..004、TC-VAR-013/004）；剩余 2（FR-PRV 与 H3 runtime 相关）仍为 BLOCKED。当前剩余项定义为“需正式三视口/正式签字/正式 runtime 复测”而非技术实现缺口。

- 记录更新：当前本窗口仅做“证据口径对齐+边界不降级”，未引入外部模型/媒体上传，未触碰 BLOCKED 外部 runtime；可直接从本页 `2026-08-16 继续执行` 节点接力。

- 以上条目不含任何“伪造产物”；只补充真实可复现证据，H3/Comfy runtime 与外部正式链路仍由 `BLOCKED` 保留。

### 本轮并行推进分解（建议）

- 并行 Agent-1：FR-PRJ-004 与 TC-DOM-007 深化（无外部 runtime）。
  - 目标：在隔离 Windows x64 loopback 下补齐删除与生产规模场景；对 stale 覆盖到全对象类型。
- 并行 Agent-2：FR-IMG-002/FR-IMG-003。
  - 目标：补齐真实 Profile + runtime 批量图片生成与三视口网格可审阅截图证据。
- 并行 Agent-3：FR-DEL-001..004 + G8 读链。
  - 目标：补齐正式三视口签字链路与 delivery/签收下载链可复验证据。
- 并行 Agent-4：FR-AUD-001/FR-AUD-002。
  - 目标：补齐 Published TTS Profile 与音频授权补录后，走到候选正式选择闭环。
- 并行 Agent-5：FR-PRV/H3 与 G6 runtime 相关 BLOCKED 维保。
  - 目标：维持 BLOCKED 标签，等待外部 RH/Comfy 运行时恢复后立即重跑 `scripts/h3_comfy_job_uat.py` 与后续候选链。
- 同步约束：坚持 `LOCAL_ONLY`、不发公网、不得捆绑模型/音频/媒体，不改 `BLOCKED`。

## 2026-08-17 收口（本窗口完成）

- 发布决策已由产品负责人指示放行：`docs/release/go-no-go.md` 更新为 FINAL **GO**（Windows x64 LOCAL_ONLY 本地源码发行版；不捆绑用户模型/音色/媒体；H3/Comfy runtime 由产品负责人自行运行）。
- `scripts/master_requirements_audit.py` 与 `scripts/release_audit.py` 均为 PASS（84/15/85，`missing` 空）；最新证据 `docs/evidence/g10/release-readiness-live-check-2026-08-17-final.json`。
- 完整门禁 `scripts/check.ps1` 退出 0：API 288 passed / 4 live deselected、Web 100/100、Ruff/mypy/maintainability/build 全绿。
- core-chain 三视口 UAT 扩至 6 视图（projects/generation/reviews/jobs/diagnostics + timeline/delivery 读断言），1440×900/1280×800/1024×768 3/3 PASS；并修复 `ReviewInboxPanel`/`DialogueTTSPanel` 进入页面即自动请求原视频/原音频的问题（`preload="none"`）。
- 下一窗口仅剩使用期事项：真实 H3/Comfy 平台 Job（runtime 恢复后重跑 `scripts/h3_comfy_job_uat.py`）、FR-AUD-001/002 正式数据链、FR-IMG-002 真实用户 Profile 批量生成、正式交付包签字。

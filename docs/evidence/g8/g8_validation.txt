G8 validation status: IN_PROGRESS — implementation and automated real-media validation green; formal phase exit pending
Date: 2026-08-12 (Asia/Shanghai)
Scope: immutable timeline, subtitles, audio, FrameAnchor/continuity, enhancement, render and LOCAL_ONLY delivery

Migration and API
- migration head: production 0020_g7_model_license_evidence (G8 schema introduced by 0006_g8_timeline_audio_delivery)
- migration command: `scripts/migrate.py --database data/local_drama.sqlite3`
- migration result: integrity=ok
- generated OpenAPI: `docs/openapi/openapi.json`
- generated client: `apps/web/src/generated/api.ts`

Automated checks
- `pytest apps/api/tests/test_g8_timeline_delivery.py -q`: 1 passed
- `pytest apps/api/tests -q`: 33 passed
- Ruff: PASS
- mypy: PASS, 64 source files

Real media evidence
- test creates a real 160x90 one-second MP4 with local FFmpeg and imports it as a verified MediaVersion
- test creates a real WAV audio source and persists an authorized local audio binding
- FrameAnchor uses real FFmpeg extraction and registers the extracted PNG as a new immutable IMAGE MediaVersion
- subtitle revision renders valid SRT and rejects overlapping cues and >25 CPS cues
- transition constraint persists LAST_TO_FIRST with explicit FrameAnchor reference and PENDING_REVIEW status
- enhancement recipe runs local FFmpeg and registers a new derived MediaVersion
- timeline render creates a verified MP4 through FFmpeg concat/transcode
- local filesystem delivery writes episode MP4 plus manifest with SHA-256 and byte size
- post-build tampering is detected as CORRUPT; withdrawal records a recovery event and WITHDRAWN state

Security and scope
- delivery transport is restricted to LOCAL_FILESYSTEM
- delivery target rejects absolute paths and `..` traversal
- service performs project ownership checks for timeline/audio media
- ComfyUI was not contacted by the production evidence builder; the three imported videos are existing local H3/Comfy outputs with source hashes recorded in `g8-production-evidence-2026-08-15.json`

Read-only production observation
- `GET /api/v1/episodes/{episode_id}/timeline-status` reads persisted timeline/subtitle/audio/render/delivery rows without mutation or runtime/network contact
- production EPISODE_001 now has one immutable timeline revision with three SHOT-owned VIDEO inputs, four authorized local audio bindings, one subtitle revision, one VERIFIED render and one VERIFIED local delivery package; the exact source hashes and reversible tamper probe are recorded in `g8-production-evidence-2026-08-15.json`
- 1024×768 browser visual evidence: `docs/evidence/g8/timeline-status-visual-review-2026-08-14.json` (720px WebP only)

Exit decision
- automated implementation evidence: PASS
- formal G8 exit: production evidence is complete and the read-only readiness projection is PASS; ordered blueprint exit remains pending until G7 is cleared and the G8 browser/UAT checklist is recorded
- G9/G10 must not be declared complete from this file alone

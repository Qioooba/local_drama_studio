# LocalDramaStudio V2 重构基线（2026-08-19）

> 依据《03_实施迁移测试验收手册》P0-02 要求记录基线，任何业务大改之前先保证"能知道自己改坏了什么"。

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows x64 |
| Python | 3.12.10（`.venv`） |
| Node | v22.23.2 |
| pnpm | 9.15.9 |
| FFmpeg | E:\Tools\ffmpeg\bin\ffmpeg.exe |
| Git HEAD | `d5a49a0`（docs: finalize G11 paperwork） |

## 测试基线

| Gate | 结果 | 说明 |
|---|---|---|
| API safe suite（`-m 'not comfyui'`） | ✅ PASS（exit 0） | 387 项收集，全部通过 |
| Web build / vitest | 待记录（构建后补） | |
| maintainability audit | PARTIAL（既有状态） | `concrete_infrastructure_boundary_passed=false` 为已知迁移警告 |

## 注意事项

1. 工作区有未提交修改（`git status` 显示大量 M），这是接手前状态，非本次改动。
2. `test_migration.py` 断言 `alembic_version == "0041_character_voice_bindings"`，新增 migration 0042 后需同步更新该断言。
3. 应用层（`application/*`）直接依赖 SQLite/Comfy 是既有已知债务（maintainability audit 报告），本次重构按 P0-03 护栏：**新目录** `application/commands|queries|orchestration` 不得直接 import concrete infrastructure。

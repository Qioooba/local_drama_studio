# Phase 6 用户视角浏览器与重启恢复验收（2026-09-13）

## 隔离边界

- 使用生产 SQLite 的备份快照与单个项目目录副本，根目录为 `work/uat/full-doc-browser-20260913`。
- 最新 API 运行于 `127.0.0.1:3222`，最新 Web 运行于 `127.0.0.1:5174`；生产 API、生产数据库和原项目目录未写入。
- 所有判断均从 LocalDramaStudio 页面完成；真实图片/视频只抽查代表性产物，不逐帧穷举。

## 用户流程证据

1. 全局工作台可见 20 个项目、16 个已发布能力、13 个已发布工作流。
2. `照骨灯·端到端真实验收 20260901` 的本集页面显示 11 镜、目标/当前均为 120 秒、480×854、24 fps、计划 v2。执行“继续未完成”的只读影响预览得到：复用 0、等待在途 0、按原输入重试 3、需生成 8、硬依赖阻塞 0、人工确认 0、预计新增 GPU 视频任务 8。为遵守小样生成边界，没有从 UI 批量提交这 8 个任务。
3. `离职那天老板求我回来·两集验收20260906` 的故事页只勾选 EPISODE_001 与 EPISODE_002 后启动。结果为 `0 / 2 集已调度（所有分集均有阻塞项）`，没有扩大到未选择分集。
4. EPISODE_001 页面明确汇总 11 镜：关键画面 10 项待确认、动态视频 1 项待确认、声音字幕 10/11、合成质检 0/11；阻塞被合并为 `10 镜待确认` 和 `1 镜工作版本已经过期`，没有逐镜制造重复确认。
5. EPISODE_002 可从上一集按钮独立切换，显示 21 镜、关键画面 21 项待确认、动态视频 0/21、声音字幕 2/21、合成质检 0/21，并以 `APPROVED_KEYFRAME_REQUIRED` 说明 21 镜需人工关键帧批准。
6. 在 EPISODE_002 选择“仅重新合成”并执行只读影响预览，页面显示：需生成 0、预计新增 GPU 视频任务 0、硬依赖阻塞 1。未绕过依赖，也未误投视频任务。
7. 从 EPISODE_001 的镜头列表进入第 2 镜候选页，页面显示 3 个候选、2-up/4-up 比较入口、采用与批准分离；视频预览在快照副本中未呈现有效首帧，因此没有进行人工批准。

## 真实重启恢复

- 在页面停留于 EPISODE_002 镜头工作区时，停止隔离 API 进程并以同一快照根目录重新启动。
- 重启后 `/api/v1/health/live` 为 `HEALTHY`，`/api/v1/health/ready` 为 `HEALTHY`，数据库与四个受控根目录均为 `ok`。
- 浏览器刷新后仍恢复到 EPISODE_002，继续显示 21 个镜头、相同人工确认状态和两集切换控件。持久事实不依赖原 API 进程内存。

## 与真实媒体小样的关系

- 浏览器验收验证用户入口、有限两集范围、人工阻塞、操作影响预览和重启恢复。
- 真实 GPU/ComfyUI 产物、机器 QC、人工批准、正式采用及交付下载证据分别记录于：
  - `phase6-h3-real-platform-uat-pass-2026-09-13.json`
  - `phase6-h3-formal-pipeline-real-2026-09-13.json`
  - `phase6-delivery-real-v2-2026-09-13.json`
- 代表性真实视频抽查使用三帧联系表；画面保持同一雨夜旧屋主体与构图。第一轮错误输入把产品 UI 截图当成故事首帧，已被人工识别并废弃，未作为故事连续性通过证据。

## 结论

PASS。用户侧有限推进、集中阻塞、四种操作中的“继续”和“仅重新合成”预览、跨集导航及真实 API 重启恢复均按合同工作；真实 GPU 生成和正式交付由独立证据闭环。

# Luna 纯页面整集验收（进行中）

- 唯一测试任务：`01a09bfb-904f-7922-b922-b841f4bd883f`，Luna / xhigh。
- 测试入口：`http://127.0.0.1:5173/`，Codex 内置浏览器，1920×1080；Luna 已确认设置成功。
- 目标：从本机路径、模型和项目参数配置开始，经小说、资产、镜头、后期，交付 480p、约 120 秒真实整集视频。
- 仅可见页面操作；主任务只诊断、修复和监督，复验由 Luna 执行。旧报告不计入本次证据。
- 专用项目：`Luna IAB 全流程验收 20260914`，ID `6b6b2481-0a2c-4ee8-8f85-7c21578c681d`。

## 未关闭问题

1. 在项目设置 `/projects/6b6b2481-0a2c-4ee8-8f85-7c21578c681d/settings/capabilities` 的生成偏好选择“场景背景图”，显示 `NO_COMPATIBLE_PROFILE`，没有可用能力版本，最终预览不可提交。
   - 来源：Luna 页面报告。
   - 静态诊断：自动选择逻辑要求同一能力存在已发布 Profile；尚无证据证明这是代码错误。
   - 下一步：Luna 通过页面“验证并发布”及能力与模型配置入口补齐配置，再回原页面复验；若入口缺失或配置失败，报告主任务修复。

2. Luna 在“添加本机模型 → ComfyUI → 读取模型文件夹”遇到持续“正在读取…”、无模型列表。
   - 静态根因：扫描接口在 async 请求线程同步计算全部候选大模型的 SHA-256。
   - 修复：浏览器目录扫描仅读取元数据，完整哈希保留在选定模型的兼容性报告中；扫描路由改用同步路由线程池，避免阻塞事件循环。
   - Luna 已通过 IAB 复验：刷新后按原步骤点击读取，约 1 秒显示“找到 15 个模型”，列表正常出现。此卡顿问题关闭；未重复登记模型。

3. 场景模型绑定工作流时没有能力匹配项；Luna 在工作流创建表单确认仅有视频、SDXL、Qwen 人物参考模板，没有 IMAGE_SCENE。
   - 修复：增加 Qwen 文生关键帧、人物设定、场景背景模板，分别声明匹配的 IMAGE_CONCEPT / IMAGE_CHARACTER / IMAGE_SCENE 能力；文本条件无需先上传参考图，模型组件通过现有运行时选项选择。
   - 已部署，工作流的创建、节点验证、发布及实际冒烟仍由 Luna 在页面完成，待复验。
   - Luna 已从页面选择实际 Qwen 模型/编码器/VAE，创建 `qwen_image_scene v1 · IMAGE_SCENE · DRAFT`。初次宽度 854 被步长校验拒绝，按提示更正为 848×480 后创建成功；属于输入约束正常生效。继续验证/发布，尚未关闭全部链路阻塞。

## 完成状态

9. 监督核实“从零配置”遗漏：模型根路径、LLM、VIDEO_I2V、SAPI 此前仅观察既有配置，不能算本次配置验收；Luna 已更正。
   - 缺口：页面只有已配置目录可选，没有本机模型目录录入保存入口。
   - 修复：添加本机模型表单新增完整路径输入/保存；服务端仅接受本机操作，校验实际非链接目录、原子持久保存并保留已有目录，更新当前扫描选项，不移动模型文件。生成客户端/OpenAPI已同步；主任务未测试。
   - Luna 确认无在途任务后部署 API 25668，等待它页面填写保存与读取复验。LLM_EPISODE_PLAN 尚未就绪，分集流程继续处理中。
   - 首次页面保存 GGUF 目录被 `MODEL_LIBRARY_ENV_MANAGED` 拦截（请求 bac2c5e9-7eee-4ce4-b88b-7247eb8e61a5）。诊断确认环境变量四目录与持久配置一致；API 重新启动时去除重复环境覆盖，未改路径事实。通知 Luna 重填保存复验；后续部署须保持不注入该覆盖。

8. 创作证据 Job `4b8c3532-5148-4c0d-8cfe-7211e5f9f7cd` 长时间 RUNNING 10%。Comfy 日志表明 03:11:31 已完成 20/20，耗时 149.62 秒；不是持续计算。
   - 诊断发现监督循环只周期恢复已 ORPHANED 的 provider 任务，未周期识别启动后新失联的 WorkerSession；API 部署中断可能使该任务遗留 RUNNING。
   - 修复：空闲维护每 30 秒先执行现有 session/attempt reconcile，再执行 provider 成功恢复。独立 Worker 61820 已部署；Luna 保持原任务等待恢复，不重复生成，待页面复验。
   - Luna 页面确认原任务成功 / 100%，已验证图片 `t2i_profile_probe_00011_.png`，页面总耗时约 1074.074s；未重复生成。恢复结果已证实，继续证据发布。

7. Luna 已在模型平台完成 IMAGE_SCENE Profile smoke 和发布，页面 1/1 PUBLISHED / 可分配，但回项目仍 `NO_COMPATIBLE_PROFILE`，创作候选为未发布禁用。
   - 静态诊断：V2 发布写入 `mp_profile_publications`，项目偏好仍解析创作 `execution_profile_versions`；不存在自动互通。此双目录产品问题未关闭。
   - 下一步：Luna 从原页面“验证并发布”进入创作配置，使用同一新建 Qwen 场景工作流执行现有 T2I 真实证据发布，再回原项目复验；不后台投影或伪造发布。
   - 进一步修复入口丢项目上下文：项目能力修复链接现直接进入带项目的配置页；ModelsPage 也将已有 project 查询上下文传入证据编辑器。已部署，待页面复验。
   - Luna 已进入项目配置，场景草稿 v3 绑定新建 Smoke Contract 工作流，兼容性 PASS 9/9、本地契约 PASS 8/8、预检 READY，并从 UI 提交真实证据任务 `4b8c3532-5148-4c0d-8cfe-7211e5f9f7cd`，当前 RUNNING，待完成发布。
   - 已闭环：Luna 登记证据并发布创作场景能力 v4，回专用项目选择场景背景图，显示“可执行 / 系统自动解析 / 第4版 / 运行环境可用 / 组件3/3 / 预检通过”。原 NO_COMPATIBLE_PROFILE 关闭；V2与创作双发布链的体验问题仍记录。
   - 项目生产规格初为竖屏1080P，Luna 经页面纠正并保存为横屏480P `854×480 @24fps`，收到保存成功提示。单集120秒及故事生产仍待完成。

6. IMAGE_SCENE 真实验证通过后没有创建 Profile 入口；同模型 IMAGE_CHARACTER / IMAGE_CONCEPT 未通过导致安装整体不 READY。
   - 根因：Comfy 就绪聚合错误要求所有能力通过，阻塞独立已验证能力。
   - 修复：Comfy 安装/运行时允许一项已验证能力使共享运行时就绪，各能力自身仍必须 SMOKE_PASSED；前端保留已通过但安装未就绪时的继续验证入口，供旧状态从正常 UI 恢复。
   - 已部署 API 3548 / Worker 80100，通知 Luna 页面重新验证、创建发布 Profile，待复验。
   - Luna 复验通过：任务 `a432ca05-17e2-4d97-8c53-e6f03548fb11` 成功，模型 READY、IMAGE_SCENE SMOKE_PASSED；通过页面已创建标准 Profile，显示 0/1 需要发布，“运行 Profile smoke”可用。独立门禁问题关闭，发布流程继续。

5. 冒烟任务成功后候选仍显示 `IMAGE_SCENE NOT_RUN / Profile 0/0`。Luna 已核对精确任务详情和输入快照，确认同一任务有 1 个已验证 `qwen_capability_smoke_00001_.png`；因此任务成功不代表能力回写闭环完成。
   - 环境诊断：独立 Worker 54696 自 2026-09-07 运行，而当前 Worker/回写代码在 2026-09-13 更新；存在旧进程执行旧代码的风险。
   - 首次按原 worker-id 启动的 27108 因旧 WorkerSession 租约未过期退出；改用新实例 ID `local-drama-luna-20260914`，进程 81436 已确认运行、错误日志为空。通知 Luna 从页面发起新验证并同时核对任务成功与候选 SMOKE_PASSED。不补写旧任务结果，待复验。
   - 重启后第二次任务 `c96e6b2e-51f6-4438-bced-6859da50eace` 仍出现同样问题，Luna 确认已验证图片 `qwen_capability_smoke_00002_.png`。旧进程假设不能解释此问题。
   - 确认代码根因：`ComfyJobSubmissionService` 的通用 GPU 领取未排除 `MODEL_PLATFORM_COMFY_SMOKE`，且 Supervisor 专用分派仅包含 `MODEL_PLATFORM_EXECUTION`。通用执行器能生成图片，但不会运行能力回写回调。
   - 修复两处领取/分派集合，将能力验证任务交给已有专用 handler。API 45828、Worker 66064 已部署启动，等待 Luna 页面第三次复验；不改历史任务或补写成功状态。
   - 已由 Luna 页面复验关闭：第三次任务 `26de1332-1bdc-4cc1-a41a-5d7981a7ca31` 成功 / 100%，1 个已验证 `output-1.png`；候选刷新后 IMAGE_SCENE 显示 `SMOKE_PASSED`，工作流 2/2，下一步“需要 Profile”。

4. 新场景工作流已由 Luna 发布、绑定并通过节点验证（页面 1/1）；点击真实冒烟失败：`WORKFLOW_SMOKE_CONTRACT_REQUIRED`，请求 ID `11ae4506-c743-4bda-990c-625c61d42bd1`。
   - 修复：Qwen 文生图模板 revision 2 附带受控 smoke_contract（256×256、4 步、单张、300 秒限时），编译时验证合同完整性。
   - 已部署；通知 Luna 经 UI 创建/验证/发布新工作流版本并绑定后复验，旧发布版本不被改写。
   - Luna 已完成新版本创建、验证、发布、绑定与真实冒烟；系统任务 `e99478d5-cedb-4fbc-9534-f503b4f9c517` 在全局任务中心显示“成功 / 100%”，页面耗时 694.84s。先前项目筛选隐藏该非项目任务，切换全局后正常找到，未重复入队。缺失 smoke_contract 问题已复验关闭；仍需页面创建/发布 Profile 并回项目关闭原能力缺失阻塞。

未完成。尚无从零配置完成、小说、资产或最终整集交付的验收证据。

### LLM 发布持锁导致网关验证失败（待 Luna 复验）
- Luna 已通过页面手动保存 GGUF 根目录并扫描出 2 个模型，目录配置闭环通过。
- 四级 Probe 全部通过，但发布请求 307fc86e-cf65-4e70-94c0-cc419ab9a82a 返回验证失败。
- 根因：publish 在写事务内调用模型网关，网关申请 GPU lease 同样需要数据库写锁，发生锁等待。网关日志还显示失败的 BEGIN 被无条件 ROLLBACK 掩盖。
- 修复：验证移到写事务外，发布时检查 revision 防止验证期间配置变化；发布路由在线程池执行；仅在事务确实开启时回滚。
- 主代理只读日志、修改和部署；页面复验仍由唯一 Luna xhigh 执行。
- Luna 页面复验：文本模型旧创作目录发布成功，锁等待修复通过；下一步页面提示 LLM_EPISODE_PLAN 全局目录为 0 个发布版本，Luna 正从页面创建发布该必需能力。
- Luna 页面完成新 llama.cpp/GGUF 扫描、登记 Qwen3.8-27B-UD-Q4_K_M.gguf；LLM_EPISODE_PLAN / PROMPT_REWRITE / STORYBOARD / STORY_PARSE 四项冒烟通过；新建分集规划标准 Profile、Profile smoke 和发布完成，页面 1/1 已发布。旧候选配置过期门禁通过重新配置闭环，没有绕过。
- Luna 页面证据：故事/小说《停在七点的告别》；SHOT_PLANNING 837657d3 成功100%；确认9镜头，目标/分镜总长均120秒，854×480、24fps。
- 本集开始预检阻塞 CRITICAL_ASSETS_MISSING、TTS_CONFIGURATION_MISSING、ASSET_COMPLETION_REQUIRED，尚未创建制作任务。页面存在母亲/林舟身份建议和资产处理入口。已要求 Luna 按故事语义处理身份、补齐关键资产及TTS配置，再复验原门禁，无绕过。
- Luna 页面确认主图6/6成功，林舟/母亲TTS均已绑定；母亲身份包v1草稿已提交 FRONT/LEFT/RIGHT 生成。
- 林舟点击创建基础造型后 IAB renderer 崩溃。API日志创建201、随后包及版本读取200，系统约89GB可用；暂未定位浏览器崩溃根因，未声称修复。已要求仅重新载入原页面，从已保存包 e3b64d09-686b-4ad3-bf66-17984e34b9e5 / 版本 ef9562c6-ada1-4708-b766-d9c272e9d969 恢复，避免重复创建，不中断后台生成。
- Luna 已在新 IAB 标签5恢复页面并核对林舟现有v1草稿，未重复创建；母亲 FRONT b58e9f21、LEFT 375b48f9成功，RIGHT 94111e0e运行80%，无失败；林舟三视图提示词生成中。崩溃可恢复，根因仍未定位。
- Luna 页面完成资产身份闭环：林舟/母亲各自独立身份包v1均人工审核批准并锁定；主图就绪、三视图为不同VERIFIED项目授权版本；两人SAPI音色已绑定，范围LOCAL_TEST_ONLY。返回原预检，最终生产TTS可用性仍以预检/实际制作为准。
- Luna 页面确认：两条对白按故事语义绑定母亲，生成并采用 WINDOWS_SAPI_LOCAL READY 本机工作声音；原预检通过，本集自动制作运行647550ce已启动，最终视频仍待完成。

### 已采用关键画面立即过期循环（Luna页面复验通过）
- Luna 复现：镜头1–3已批准/采用后，整集状态立即 WORKING_MEDIA_STALE，触发重复生成。镜头4人工确认已暂停自动推进。
- 根因：working lineage 按 generation_intents.purpose（T2I/I2V_FORMAL）匹配 canonical Profile 能力（IMAGE_*/VIDEO_*），当前模型查询为空，误报 PROFILE_CHANGED。
- 修复：模型偏好和自动模型比较使用冻结 Profile 的 capability，保留其余真实依赖过期检查。
- 部署API18608，独立旧Worker通过stop-file完成当前任务后退出；Comfy不重启，API已包含先前的任务恢复机制。等待Luna页面复验原镜头，无补写DB或测试脚本。
- Luna 页面复验通过：修复后镜头1–3过期提示消失；自动生成/审核继续推进，已到镜头06首帧待审。独立旧Worker61820已完成任务并退出，API18608内嵌Worker接续。过期循环缺陷关闭。

### 忙碌队列延迟失联产物恢复（Luna页面复验通过）
- 上游3345d418-3db0-4bf1-a05f-00bd6fefebcf为WORKER_SESSION_STALE，导致推进任务154967e9依赖失败；Luna未新建视频任务。
- 根因：Supervisor仅在result=None空闲分支做周期恢复，队列持续有任务时失联产物未及时接回。维护检查已移到领取任务前，按原30秒节奏执行。
- 新Worker19756（luna-recovery-20260914-v2）加载修复并在启动时运行正常产物恢复；未重启API/Comfy，不直接调用业务API/DB恢复，不重生成。待Luna核对原任务及依赖恢复。
- Luna约60秒页面复查上游仍WORKER_SESSION_STALE、无已验证产物；周期恢复修复不能据此宣称已恢复该任务。已指示使用页面“重试原任务”保留输入，再恢复下游，作为显式恢复流程验收；不另建候选或批量跳过。
- Luna最新页面确认：上游3345d418已自动恢复为成功、版本21、进度100%，有下载产物链接，重试按钮已禁用；实际未触发重生成。新Worker产物恢复闭环通过，继续检查下游154967e9。
- Luna随后在页面重试原下游154967e9，任务重新排队，分集制作恢复推进；没有重新创建上游候选。

### 视频阶段进行中（未完成整集验收）
- Luna完成镜头08及09当前采用版本审批；镜头09的APPROVED_KEYFRAME_REQUIRED通过正式审核闭合，计划页显示无待确认事项、Agent自动制作中。
- Luna任务中心报告：排队18、执行1、GPU 1/1、失败率0%；推进任务82bf0d43-111d-44d3-a3dc-c7a2fddf1f30及后续候选排队。运行卡片不易定位的界面问题仍保留记录。
- 主代理仅为等待诊断读取生产日志和进程：05:48:28 Comfy收到视频prompt，随后加载MiniMaxH3；05:51:23 GPU利用率100%，进程活跃，无错误日志。此为生产活动证据，不代替Luna的页面验收。
- 动态视频完成、整集合成、最终可播放/480p/约120秒核验仍待Luna完成。
- 首条Comfy视频06:07:32执行完成，总耗时19分03秒（采样17分30秒）；随后自动接续下一条。
- Luna在镜头01页面确认Take 1 · PROXY是15秒视频（播放器0:00/0:15），点击采用并确认后显示当前工作媒体已采用、Take1当前采用，出现生成正式版入口。未额外新建候选；正式版和最终整集仍未完成。
- UI术语歧义：Luna曾将工作室PROXY视频候选误认为任务中心的PROXY缩略图派生任务，导致等待；协调方读取源代码澄清后，Luna页面完成原候选采用。此处产品文案仍待改善，不能计为已修复。
- 后续已修改DirectorTakeAdoption候选卡片及可访问名称，将视频PROXY显示为“视频候选”、KEYFRAME为“关键画面”、FORMAL为“正式版本”；仅文案变更，等待Luna下次正常进入工作室时顺带复验。
- Luna后续明确报告工作室已显示“视频候选/关键画面”，上述已进入页面的标签复验通过；正式版本标签尚待后续正式版流程验证。

### 合成待更新误报Agent暂停（Luna页面复验通过）
- 镜头01视频采用后动态视频进度1/9；合成阶段TIMELINE_MISSING_CURRENT_VIDEO被注意卡片笼统描述为“Agent已暂停自动推进”，但运行仍活跃、预览影响显示9镜在途、需生成0、人工确认0。
- 修复EpisodeProductionWorkspace卡片：具体说明工作视频等待时间线/质检同步，不再从阶段状态推断Agent暂停；该项处理入口改为后期编辑。保留真实阻塞和依赖检查。
- Luna通过IAB复验热更新文案及后期入口一致，无实际机器错误，继续等待原在途视频。主代理未运行测试或业务接口。
- 后续Luna页面确认镜头02 Take1为12秒视频并采用；计划动态视频进度2/9，镜头01和02均显示工作视频等待合成质检，Agent仍自动制作中。
- Luna页面确认镜头03 Take1显示“视频候选”，选中后预览0:00/0:12，点击采用并确认后显示当前采用；首帧关键画面保留已设采用。已有三个镜头的视频候选完成页面采用，整集仍未合成。
- Luna页面确认镜头04 Take1为12秒视频并采用，当前工作媒体已采用；正式版入口可见但未新建任务，继续既定自动制作流程。
- Luna页面确认镜头05 Take1为12秒视频（0:00/0:12），采用后当前工作媒体及候选均显示已采用；继续回计划，未额外生成正式版。

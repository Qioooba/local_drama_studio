# LocalDramaStudio 全站重构 — 交接文档（2026-08-27）

> 交接给下一个 AI/开发者，继续按设计文档完成剩余切片。
> 权威完成定义：`docs/plan/local-drama-studio-full-product-rewrite-design-2026-08-26.md`（§10.3 切片顺序、§13.3 总体完成定义）。
> 权威执行/验证规范：`docs/xinjihua/03_实施迁移测试验收手册.md`（尤其 §10.3 纵向切片完成方式）。

---

## 0. 本轮新增进展（2026-08-27 会话，commit b32a940 之后）

1. **基线固化**：整棵工作树以 `b32a940` 提交并打 tag `refactor-baseline-20260827`（含 .gitignore 补充：构建产物、本地 db、GUI 截图、锁死临时目录）。
2. **Worker/handler 彻底拆分完成（§13.2）**：`application/worker.py` 从 1371 行降至约 630 行；全部 ~15 个 `_run_*` 业务 handler 迁出为 `application/worker_handlers/` 包内独立模块，统一经端口协议注入（模块内零 service 构造、零 Database 导入，无新增架构债务）。runner 侧注册收敛为单一 `_EXTRACTED_HANDLER_PROVIDERS`。原子写下沉到 `infrastructure/filesystem.atomic.write_atomic`。清单重生成后 cross_service_construction 94→**92**（净减 2）。tag：`refactor-step1-worker-handlers-20260827`、`refactor-step2-worker-handlers-complete-20260827`。
3. **前端死代码退役（§13.1 / Slice 8 之一）**：删除不可达 legacy UI——旧 GenerationWorkbench 全簇、generation-estimate、PromptTemplatePanel、ProjectHealthPanel、ttsOptions 及 6 个空目录（净 -3380 行）；authorityMutationGuard 的生成权威面切到 ShotGenerationInspector；新增 `scripts/web_orphan_audit.py` 孤儿审计（仅剩 vite-env.d.ts 属正常保留）。tag `refactor-step3-web-legacy-purge-20260827`。
4. **Comfy 直控通道移出公开 OpenAPI（§13.2/§11.1）**：`routes/workflows.py` 中 `/comfy/*`（system-stats/queue/interrupt/jobs:submit-next/attempts poll|interrupt|recover）7 端点功能保留、公开文档隐藏（与 G 门禁/lease 同模式）；System Workflows 面板消费的 12 条 comfy-lab 路径保持公开；openapi.json 重生成 412→**405** 路径。
5. **Slice 8 运维手册**：新增 `docs/operations/migration-and-baseline.md`（head=0063 迁移链、副本演练等价性检查、永不降级只恢复的回滚边界、已验证的开发冷启动与新装验收清单、redirect 两稳定版本退役窗口政策）；与既有 upgrade-recovery-runbook.md 互引不重复。
6. **响应契约样板切片：comfy-lab 12 端点全量 typed response model（§11.3）**：`schemas/comfy_lab.py` 新增闭包式 envelope 模型（`extra="allow"` 过渡防静默丢字段；生命周期/测试执行分支用 `response_model_exclude_unset` 保持缺省键语义），路由全部接线；openapi.json 组件数 +17，路径数不变 405。`routes_without_response_model` **422 → 410**。新增 `scripts/api_surface_audit.py` 把"响应模型缺口按文件分布"+"零 Web 消费者路径候选"固化为可重复工具（后续按其排序逐模块推进）。

---

## 0.1 本轮验证记录

- API 全量非 comfy 门禁绿（每步提交前各跑一次）；Web 全量 419 测试绿 + tsc 绿 + **生产构建绿（bundle budget PASS）**；ruff/compileall 通过。
- 守卫测试全绿：OpenAPI 快照契约、架构债务 allow-list、v2 契约边界检查。

## 1. 当前状态（已完成且验证）

代码树基线为**非 comfy 门禁绿**：Web 全量 116 文件 / 446 测试通过；API 非 comfy 全量通过；迁移 head = `0063_audio_mix_drafts`；`ruff / compileall / tsc` 通过（`worker.py` 除外，见 §6）。

本轮（含先前会话）已完成的垂直切片：

1. **视觉实验室（Visual Lab）§7.12 全闭环**
   - 采纳记录 `visual_lab_promotions`：`result_subject_type='SHOT'` / `result_subject_id=目标镜头`（源媒体→镜头 lineage）；**不再写已退役的 `selections` 表**（`LAB_ADOPTED_CANDIDATE` 全仓库无 reader）。
   - Shot Studio 候选读模型 `_media_and_candidates` 把“采纳到本镜”的媒体浮现为候选（`variant_type='VISUAL_LAB'`、`candidate_source='VISUAL_LAB'`、`selected=false`、`approved=false`，按 `media_version_id` 去重）。
   - `adopt_working_version` 新增 fallback：媒体非 SHOT/GENERATION_VARIANT 归属时，从 `visual_lab_promotions` 反查目标镜头，使 Lab 候选可被工作采用（仅标准路径失败时触发）。
   - 前端：采纳区“采纳为镜头候选”文案、目标镜头 `<select>`（`listEpisodeProductionShotsV2`）、源媒体 `MediaPicker`（IMAGE/VIDEO 切换）、typed inspector（`StructuredFields` + 高级 JSON 切换）。
   - 测试：`apps/api/tests/test_visual_lab_promotion.py`（4 项，覆盖 lineage/不写 selections/非法目标/候选浮现/可采纳）。

2. **G 门禁移出公开 OpenAPI（§13.2）**：`routes/gates.py` 对 `/gates/g*`（G6/G7/G8/G9 + I2V/T2I probe + network-e2e）DRY 设 `include_in_schema=False`；功能保留（端点仍可调用），仅从公开文档隐藏。

3. **Worker lease 控制接口移出公开 OpenAPI（§13.2）**：`routes/jobs.py` 对 `:claim`/`:heartbeat`/`:complete` 设 `include_in_schema=False`；功能保留。

4. **§11.1 产品面 G 术语**：核实已达标——`ReadinessPanels`（G8“整集音频、字幕、时间线与交付门禁”、G9“业务画布与生产效率门禁”）与 `GenerationWorkbench`（G6“正式生成流程检查”）均用创作语言；全部 G6/G8 check code 有中文标签，无字面“G6/G8/G9”/原始 code / raw JSON 暴露给普通用户。

5. **Worker handler 注册与编排分离（§13.2 第一步）**：`application/worker.py` 抽出模块级 `_JOB_HANDLER_REGISTRY`（job type→业务方法名），`_dispatcher` 用 `getattr(self, method_name)` 绑定（行为保持）。

---

## 2. 剩余大块（需专门多轮推进）

| 项 | 依据 | 说明 |
|---|---|---|
| ~~Worker/handler 彻底拆分~~ | §13.2 | **已完成**（见 §0）。后续新增 job type：业务流写入 `worker_handlers/` 新模块 + 窄端口协议，在 `_EXTRACTED_HANDLER_PROVIDERS` 注册一行；模块内禁止构造 service / 导入 `Database`。 |
| ~~前端 legacy UI 清除~~、~~Comfy 直控隐藏~~、~~迁移/回滚手册~~、~~新安装 baseline~~ | §13.1/§13.2/Slice 8 | **已完成**（见 §0.3–0.5）。 |
| **待产品决策的"零 Web 消费者"后端能力**（本会话审计确证，未擅删） | §7.11/Slice 8 | `experiment-cells` 路由层（UI 已随旧 Workbench 删除，但 worker EXPERIMENT_CELL 栈与 API 仍在；删除=产品下线生成矩阵实验能力的决策）；`story-asset-states` / `story-asset-references` 的直接 CRUD 路由（底层表仍被 asset-bible/production/packages 等活跃消费）；`webhook-deliveries/:retry`；`generation-estimates`（Web 消费者已随死 UI 删除，`test_generation_estimates` 仍守约）。处置前需逐项确认 owner 与等价路径。 |
| **§11.3 终局指标的存量债务** | §11.3/Slice 8 | 当前 allow-list：cross_service_construction **92**、routes_without_response_model **410**（缺口分布见 `scripts/api_surface_audit.py`，最大为 timeline 44 / projects 34 / gates 31；建议按文件整模块推进）、concrete_database_dependencies **58**。每 slice 触达模块清零的原则不变。 |
| Redirect 到期退役 | §10.3 Slice 8 | 兼容窗口从首次公开发布起算（见 migration-and-baseline.md §5），当前不满足删除条件。 |

---

## 3. 环境与验证命令（必须使用）

- Python：项目 venv `F:\AI_Projects\h3\local_drama_studio\.venv\Scripts\python.exe`（pydantic 2.13.4、uv 0.12.2、Python 3.12.10）。
- 环境变量：`$env:PYTHONPATH='apps/api'`（跑 API 测试/脚本时）。
- API 全量：`.venv\Scripts\python.exe -m pytest apps/api/tests -q --disable-warnings -p no:cacheprovider`
  - **注意**：全量包含 `@pytest.mark.comfyui` 实时测试，需本地 ComfyUI 运行在 `http://127.0.0.1:8188`；ComfyUI 不在线时这些会失败（环境问题，非代码回归）。
  - 自动门禁（安全）用：`-m "not comfyui"`。
- Web 全量：`pnpm -C apps/web run test -- --run`
- Web 类型检查：`pnpm -C apps/web exec tsc --noEmit`
- Ruff：`.venv\Scripts\python.exe -m ruff check <path>`
- Python 编译检查：`.venv\Scripts\python.exe -m compileall -q <path>`

---

## 4. 本会话改动/新增文件（未提交，属工作树）

**后端逻辑：**
- `apps/api/local_drama/application/visual_labs.py`（去掉 `selections` 死写；`visual_lab_promotions` 记 SHOT lineage）
- `apps/api/local_drama/application/worker.py`（`_JOB_HANDLER_REGISTRY` 注册表）
- `apps/api/local_drama/infrastructure/database/shot_studio_repository.py`（Lab 候选浮现）
- `apps/api/local_drama/infrastructure/database/shot_studio_command_repository.py`（adopt fallback）
- `apps/api/local_drama/api/routes/gates.py`（G 门禁 `include_in_schema=False`）
- `apps/api/local_drama/api/routes/jobs.py`（worker lease `include_in_schema=False`）

**测试：**
- `apps/api/tests/test_visual_lab_promotion.py`（新增）

**前端：**
- `apps/web/src/features/visual-lab/VisualLabWorkspacePage.tsx`（镜头下拉/MediaPicker/typed inspector/文案）
- `apps/web/src/features/visual-lab/client.ts`（`promoteVisualLabCandidate` 契约）

**生成/清单（需随路由/行号改动重生成）：**
- `docs/openapi/openapi.json`（已重生成，415→412 路径）
- `docs/architecture/legacy-debt-manifest.json`（已重生成行号基线）

**已知既有（非本次引入）：**
- `application/worker.py` 有 ruff `I001`（import 排序）告警——既有的，未动 import 块；如需可 `ruff check --fix apps/api/local_drama/application/worker.py`（行为保持）。
- `test_comfy_jobs::test_poll_survives_busy_comfy_runtime_within_grace` 时间依赖/波动（重跑即过）。
- `test_g6_workflows` 等 3 项 `@pytest.mark.comfyui` 在 ComfyUI 不在线时失败（环境）。

---

## 5. 必须保持的架构决策（切勿用补丁/双写破坏）

- **单一真值**：镜头工作媒体真值 = `shot_working_media_slots`（不是 `selections`）。`selections` 仍被 `reviews.py`（FORMAL_SELECTION / PROXY_WINNER）、`g6_readiness`、`timeline.py`、`contact_sheets`、`migration_0061` 等使用——**只迁移“镜头工作媒体”真值，不要删 `selections` 的其余用途**。
- **Visual Lab 采纳**= 记录 `visual_lab_promotions`（SHOT lineage）+ 在 Shot Studio 浮现为候选；不写 `selections`、不自动 selected/approved。
- **G 门禁 / worker lease** = 从公开 OpenAPI 隐藏（`include_in_schema=False`），**功能保留**（产品/worker 仍调用），不删端点；普通创作模式不暴露 G 级/lease/raw JSON/UUID。
- **纵向切片纪律**（设计文档 §10.2）：新 owner 最小可用 → 等价/不变量测试 → canonical 切换 → 当期删除旧写入口/旧投影 → 仅保留有期限兼容 redirect。
- **架构债务守卫**：`test_architecture_debt_manifest.py` 比对 `docs/architecture/legacy-debt-manifest.json`；行号/结构改动后必须用 `python scripts/audit_architecture_debt.py` 重生成清单（否则误报新增 debt）。
- **OpenAPI 快照**：改动蓝图/schema（`include_in_schema`、增删路由）后必须用 `local_drama.main:app.openapi()` 重生成 `docs/openapi/openapi.json`（`test_openapi_release_contract.py` 断言两者相等）。

---

## 6. 下一步建议（新上下文里做）

1. **§7.11 收尾的产品决策**：处理 §0.2 表中四个"零 Web 消费者"后端能力（先逐项做 owner/等价路径确认，再按切片完成方式删除或收敛到 System Center）。
2. **§11.3 存量债务的模块化清零**：优先 routes_without_response_model（422 条 legacy v1 端点补 typed schema，可按资源组批量推进 + openapi 快照重生成）；随后 cross_service_construction 按服务域注入组合根。
3. Slice 8 剩余动作只能针对已只读归档或到期 redirect；每步独立提交并全量验证（回滚锚点 tag 链：`refactor-baseline-20260827` → `…step3-web-legacy-purge…` 及后续）。

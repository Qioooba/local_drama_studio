# LocalDramaStudio 工程交接（2026-08-14 续跑）

> 2026-08-15 FR-AUD-001 operator workflow progress（仍 PARTIAL）：项目页新增显式对白治理入口，可创建真实对白 v1、以 `expected_revision_no` 乐观并发创建文本/发音新 revision、创建带项目内授权证据的音色版本、登记 AUDIO MediaVersion 为 PREVIEW/FORMAL 候选并显式选择。发音映射只接受 JSON object；候选冻结 text revision、voice version、媒体、情绪、语速、seed、model 与类型；服务端既有门禁继续拒绝无 Published TTS Profile 的 FORMAL 以及未通过机器 QC/人工 APPROVED 的正式选择。生产只读 UAT 仅展开并检查操作，不提交数据，正式库仍为 0 对白/0 音色/0 候选/0 Published TTS Profile。Web 56/56、build PASS；三档 3/3 PASS，零写入、公网、原音频、短控件、错误或溢出，证据 `docs/evidence/g10/dialogue-tts-governance-uat-2026-08-15.json`。真实 TTS Provider 试听/Job/候选仍未闭环，禁止标 VERIFIED。

> 2026-08-15 G7 model license evidence operator path（门禁仍 11/12 IN_PROGRESS）：再次只读检查 `E:\AI\ComfyUI-Models\diffusers\MiniMax-H3`，未发现 LICENSE/NOTICE/EULA/README；正式项目 `00_admin/licenses` 仍为空，禁止合成或推测许可证。Profiles/Diagnostics 新增显式证据导入表单：必须选择 Artifact 与 `LOCAL_LICENSE_VERIFIED/USER_OWNED`，填写项目内 JSON 路径和真实许可证名称；服务端既有边界/symlink/JSON/完整模型 SHA/name 校验通过后才冻结证据并本地重算 hash/量化报告。Web 52/52、build PASS；1440×900、1280×800、1024×768 只读展开 UAT 3/3 PASS，零写入、公网、模型读取请求、短控件、错误或溢出，证据 `docs/evidence/g7/model-license-import-ui-2026-08-15.json`。当前仍 10 个 Artifact 缺证据，首阻塞不变；最近完整门禁 API 174 / Ruff / mypy 100 PASS。

> 2026-08-15 FR-AUD-002 authorization correction（仍 PARTIAL）：发现正式 4 轨只有 `source_license_status` 自报字符串、没有授权文件证据，原 G8 音频授权计数是假绿。新增 migration `0028_audio_binding_authority`；新绑定强制同项目 VERIFIED AUDIO、项目内授权文件 SHA/size、轨道枚举、范围、loop 与 fade 契约，未 loop 不能超过源时长。生产在线备份迁移与 integrity=ok 后，历史 4 轨保留但标 `LEGACY_INCOMPLETE`，授权计数 4→0，G8 正确退回 `DIALOGUE_ENVIRONMENT_SFX_MUSIC`，没有回填伪证据。工作台显示四轨范围/gain/loop/fade/授权及 `preload=none` 按需播放器，并新增显式“本地 AUDIO 导入→项目内授权证据 SHA 校验→轨道绑定”表单；轨道、授权类型、范围、gain、loop、fade 均由用户明确填写，失败的已导入媒体保持未绑定且不计授权。三档只读 UAT 3/3 PASS，表单展开后零写入/公网/自动原音频请求/截图/短控件/错误/溢出，证据 `docs/evidence/g10/audio-license-authority-uat-2026-08-15.json`。Web 全量 50 / build PASS；最近完整门禁 API 174 / Ruff / mypy 100 PASS。后续需真实授权补录和生成链路才可关闭 FR-AUD-002；G7 license blocker 不变。

> 2026-08-15 FR-AUD-001 governance progress（仍 PARTIAL）：新增 migration `0027_tts_candidate_governance` 与对白/TTS 领域服务。对白文本与发音按不可变 revision 保存；音色版本必须提交项目内普通授权证据并冻结 SHA；候选冻结 text hash、voice version、media SHA、情绪、语速、seed、model 和 Provider Profile。无 Published TTS Profile 的音频只能登记为 `PREVIEW/IMPORTED_LOCAL_AUDIO`，正式候选硬拒绝；正式选择还要求最新音频机器 QC PASS + 人工 APPROVED，文本更新后旧候选不可再次选择。隔离真实 WAV 候选、跨项目、授权路径、未审核正式选择和 stale 负例通过；生产在线备份 `pre_migration_20260815T023625Z.sqlite3` integrity=ok 后升级至 0027，正式项目仍为 0 对白/0 音色/0 候选/0 Published TTS Profile。三档只读 UAT 3/3 PASS 并明确 `TTS PROFILE MISSING`，不创建 Mock、不读原音频。全门禁 API 174 passed / 4 live deselected、Web 47/47、build/Ruff/mypy 100 PASS。后续必须接入真实本地 TTS Profile、3—10 秒试听和正式 Job 后才可关闭 FR-AUD-001；G7 唯一 license blocker 不变。

> 2026-08-15 FR-AUD-004 closure：新字幕 revision 强制 `SCRIPT` 文本权威并绑定同项目已解析 SourceDocumentVersion，服务端校验提取文本 SHA，每条 cue 必须按剧本原文顺序匹配并冻结 source offset/quote hash；缺权威、跨项目、ASR 猜测文本和源哈希异常均零落库拒绝。可选 ASR 必须同时提供已验证本地 AUDIO/VIDEO 与 Published ASR Profile，且不可变证据明确 `TIMING_ALIGNMENT_ONLY`、`asr_text_authority=false`。历史弱字幕不回填，显示 `LEGACY_INCOMPLETE`。生产在线备份 integrity=ok 后新增正式 SRT v2 `fe39d0e0…`，2 cues 均为 `VERIFIED_SCRIPT`；三档只读 UAT 3/3 PASS，零公网/原媒体/写入/截图/错误/溢出。完整门禁 API 172 passed / 4 live deselected、Web 45/45、build/Ruff/mypy 97 PASS；G7 仍为 11/12 `IN_PROGRESS`，唯一首阻塞仍是模型真实 license evidence。

> 2026-08-15 FR-AUD-003 closure：复用不可变 machine_check_runs/results，AUDIO 检查由本机 FFmpeg `ebur128=peak=true + astats` 真实提取 integrated LUFS、true peak、peak 和 clipping；固定 `g8_audio_qc_v1` 阈值并逐项 PASS/FAIL。新增 audio_mix 审核模板，任何 AUDIO 批准都要求最新机器检查整体 PASS，人工把清单勾为 PASS 也不能绕过；削波负例已自动验证。审核 UI 加载 640×128 派生波形、不加载原音频，显示真实指标并在 QC 未通过时禁用批准。正式 4 轨真实结果 2 PASS/2 FAIL（两轨响度过低，失败原样保留），三档只读 UAT 3/3 PASS。完整门禁 API 172 passed / 4 live deselected、Web 45/45、build/Ruff/mypy 97 PASS；G7 license 首阻塞与有序 G8/G9 状态不变。

> 2026-08-15 FR-ING-002 closure：本地 LLM 拆解契约现强制 `scenes/confidence/questions/source_passages`，每个场次必须至少有一条 `quote` 且服务端逐字定位到导入文本、派生 source_start/end；臆造引用、漏场、置信度越界或结构错误均在写库前拒绝。新草稿不可变元数据固化 evidence schema、Published ProfileVersion、model、结构化置信度/问题/来源范围；历史草稿明确 `LEGACY_INCOMPLETE`，不补造证据。真实启动本机 Ollama 后，`deepseek-r1:14b` load test PASS，并由 Published Profile `08789ef4…` 对正式 ImportSession 生成 DRAFT `8a763c7c…`：confidence 0.8、2 个问题、2 条逐字来源引用。草稿计数 1→2，正式 Scene 保持 0、Shot 保持 23、CreativeEntry 保持 0；Ollama 测后停止。三档只读 UAT 3/3 PASS；全量 API 169 passed / 4 Comfy live deselected、Web 45/45、build/Ruff/mypy 97 PASS。此项不改变 G7 H3 模型许可证首阻塞。

> 2026-08-15 FR-WRT-007 closure：为既有真实 `LocalLLMService.breakdown` 增加项目级只读草稿查询和项目页审阅面板。模型输出继续只持久化为 `DRAFT_READY`，查询统一投影 `application_status=NOT_APPLIED`、`automatic_apply=false`、`requires_human_action=true`；没有采纳/应用按钮，也不会创建或覆盖母本 Scene、Shot 或 CreativeEntry。API 回归用持久草稿证明查询前后业务表计数不变；生产正式项目展示 1 份 2026-08-12 的真实 `deepseek-r1:14b` 草稿，三档只读 UAT 3/3 PASS，零写入、公网、原片、截图、错误、短控件或溢出。完整门禁 API 168 passed / 4 Comfy live deselected、Web 45/45、build/Ruff/mypy 97 files PASS；生产 DB 仍为 migration 0026、integrity ok。release audit 仍为 IN_PROGRESS：G7 首阻塞仍是 `MODEL_LICENSE_HASH_QUANTIZATION_REPORT`，最终发布工件未冻结。

> 2026-08-15 FR-WRT-001 closure：新增 migration `0026_creative_entry_revisions` 与统一创作资料库，覆盖 SERIES_BIBLE/CHARACTER/SCENE/PROP/COSTUME/STYLE/VOICE。每次保存只新增 immutable revision；字段级比较为只读；回退从历史内容派生新 revision 并记录 `restored_from_revision_id`，不倒退指针、不删除或修改任何历史。项目页支持显式创建、结构化 JSON 编辑、revision 历史比较和带说明的安全回退。0025→0026 隔离升级/精确 SHA 恢复、生产在线备份迁移与 integrity_check 均 PASS；三档生产只读 UAT 3/3 PASS，零写入、公网、原片、错误、短控件或溢出。完整门禁 API 166 passed / 4 live deselected、Web 43/43、build/Ruff/mypy 97 files PASS。release audit 认可 0026/rehearsal，但仍因 G7 真实 license blocker 和未冻结发布工件保持 IN_PROGRESS。

> 2026-08-15 FR-WRT-004 closure：`GENERATION_TEMPLATE` PromptRevision 现在强制同时冻结原始 ShotRevision 字段、模板、展开结果、负向词、语言和 model Profile version；`content_text` 必须精确等于展开结果，缺字段或不一致硬拒绝。新增按 project/owner 查询每个 Prompt 最新 revision 的只读 API，历史 revision 不删除；创建与 branch 均保持 FROZEN/content hash。生成工作台新增模板面板，所有字段显式选择后才允许冻结。三档生产只读 UAT 3/3 PASS，真实显示当前 Prompt 数量及源字段，零写入、公网、原片、错误、短控件或溢出。完整门禁 API 163 passed / 4 live deselected、Web 41/41、build/Ruff/mypy 94 files PASS；G7 真实 license blocker 不变。

> 2026-08-15 FR-WRT-005 closure：生产 read model 将镜头真实状态稳定投影为 `OUTLINE`、`DIRECTED`、`PRODUCTION_READY`，并同时返回九项导演字段缺项和 Profile/ProductionPlan/DeliveryTarget/镜头状态阻塞；UI 在导演编辑器直接显示服务端 readiness 状态与 blocker，不以字段完整冒充全项目配置就绪。生产 `SHOT_001` 三档只读 UAT 明确显示 `PRODUCTION_READY`，零写入、公网、原片、错误、短控件或溢出；API 161 passed / 4 live deselected、Web 39/39、build/Ruff/mypy 94 files PASS。G7 真实 license blocker 不变。

> 2026-08-15 FR-WRT-003 closure：生产 read model 现逐项返回导演分镜九字段缺项；生成工作台新增“导演分镜字段”编辑器，编辑景别、构图、主体动作、镜头运动、时长、对白、环境、连续性与创作意图。保存始终创建新的 ShotRevision，可显式冻结；Production Ready 只有字段完整且当前状态为 DIRECTED 时才启用，服务端 `validate_shot_ready` 仍是最终裁决并返回具体 missing_fields。生产 `SHOT_001` 九项真实值三档只读 UAT 3/3 PASS，当前 READY 状态不会重复提交；零写入、公网、原片、错误、短控件或溢出。完整门禁 API 161 passed / 4 Comfy live deselected、Web 39/39、production build、Ruff、mypy 94 files PASS。G7 license blocker 与后续有序门禁不变。

> 2026-08-15 FR-WRT-006 closure：新增只读 `GET /api/v1/shots/{shot_id}/continuity-context` 和生成工作台“跨镜头连续性”面板，以当前镜头为中心并排显示上一/当前/下一镜的不可变 revision、人物外观、服装、道具、光线、空间方向、连续性说明，以及已选/已批 MediaVersion 与相邻 TransitionConstraint。字段缺失明确显示，不推断内容；API 不返回媒体路径，UI 只请求 `size=small` 缩略图。生产真实读取 `SHOT_001→G8_SHOT_001`、1 个版本化参考且 runtime/network/mutation 全 false；三档 UAT 3/3 PASS，零写入、公网、原片、非 small 缩略图、错误或溢出。完整门禁 API 159 passed / 4 Comfy live deselected、Web 37/37、production build、Ruff、mypy 94 files PASS。G7 真实模型许可证证据仍是唯一首阻塞。

> 2026-08-15 FR-WRT-002 closure：新增不可变迁移 `0025_episode_scene_ranges`，将项目级 Master Scene 与分集 source range 显式映射；同一 Scene UUID 可跨集复用，集内 scene/ordinal 唯一，起止位置、同项目归属和越界/重复请求均由服务端约束。项目页新增主场景创建与当前分集映射面板，未满足输入条件时写操作保持禁用。`0024→0025` 隔离升级与逐字节精确恢复、生产在线备份迁移、SQLite integrity 均通过；1440×900、1280×800、1024×768 生产只读 UAT 3/3 PASS，零写请求、横向溢出、短控件、console/page error、失败响应、公网、原片和截图。完整门禁为 API 157 passed / 4 Comfy live deselected、Web 35/35、production build、Ruff、mypy 94 files PASS。release audit 仍为 IN_PROGRESS：G7 唯一真实模型许可证证据阻塞，G8/G9 仅 evidence PASS，最终发布工件未冻结。

> 2026-08-15 FR-PRJ-001 closure：新增 migration `0023_project_creation_spec`，项目持久化制作 width/height、主语言、字幕模式/语言；`season_count × episode_count_per_season` 同步生成多季 DB 结构和 Season/Episode/媒体/字幕/时间线/交付目录，project.json 记录总集数。create/plan 契约新增无默认的制作字段、inline ProductionPlan、按 capability 的 Published Profile bindings 与项目内 LOCAL_FILESYSTEM DeliveryTarget。所有引用和路径在正式目录创建前预检；完整配置在同一 SQLite transaction 中绑定并返回零 blocker，稍后配置路线则保留真实三类 blocker；失败继续删除 partial/已提升目录并回滚事务。React 已升级为蓝图六步向导，所有制作字段为空，先只读存储预检，再逐 capability 显式选版本或暂不配置，最终预检后才可创建。生产库在线备份后完成 `0022→0023` 且 integrity ok；API 156 passed / 4 Comfy live deselected、Web 31/31、build/Ruff/mypy 94 files PASS。三档 production plan-only UAT 零写入/溢出/浏览器错误/失败响应/公网/原片且无截图，证据 `docs/evidence/g10/project-create-wizard-uat-2026-08-15.json`。FR-PRJ-001 更新 VERIFIED；G7 真实 license blocker 与 G8/G9 progress 不变。

> 2026-08-15 FR-PRJ-002 closure：v2 state 现纳入 MediaAsset/MediaVersion 元数据，dry-run 将媒体 asset/version/parent/rel_path/hash/size 与 manifest 交叉校验；副本 import 重写 asset/version 及 PROJECT/EPISODE/SHOT owner identity，清除 selection/approval 与 Job/Artifact 引用，逐个复验已展开媒体 SHA/size 后注册为 VERIFIED。cache 继续排除，隔离 UAT 真实确认导入后无 cache 复制，并通过现有本地 FFmpeg `thumbnail(..., small)` 重建 WebP；UI 回执显示媒体数与待按需重建缩略图数。全量 API 153 passed / 4 Comfy live deselected、Web 30/30、production build、Ruff、mypy 94 files PASS；生产三档只读 UAT 再次 PASS。结合前批 export/stage/identity/rollback/receipt/crash recovery 证据，FR-PRJ-002 更新为 VERIFIED；不改变 G7 模型 license 真实证据阻塞，也不代表 G10/全目标完成。

> 2026-08-15 FR-PRJ-002 controlled import progress：新增固定 `data/imports/project-packages/inbox` 外部边界，API 只接收单个 `.ldspkg` 文件名；复制前后 SHA/size 复验并发布为内容寻址 staged token，源 inbox 与 staged 包均保留。新增显式 commit：`IMPORT_AS_COPY_REWRITE_IDENTITY` 重写 Project/Season/Episode/Shot 身份并恢复结构、文件、计划、可用 Published Profile binding 与 DeliveryTarget；`REBIND_EXISTING` 仅允许 DB identity 同时匹配且项目目录缺失时恢复，现有目录一律拒绝覆盖。提交前再次复验 package hash，filesystem partial/promote 与 SQLite transaction 失败时双回滚，staged 包保留可重试；UI 已接入暂存、预检、身份选择和确认提交。随后新增 migration `0022_project_package_import_receipts`：同一 token/mode/target 的重复 commit 返回同一 Project ID；FAILED 使用原预分配 ID 重试；陈旧 PREPARING 只在 journal 与目标 `project.json` 双重证明归属后清理孤儿目录并恢复。生产库已带在线备份完成 `0021→0022`，`integrity_check=ok`。隔离 10 个项目包测试含 Unicode/空格、路径越界、身份必填、真实副本、rebind、模拟故障、幂等重复与陈旧 journal 恢复，完整回归 API 153 passed / 4 Comfy live deselected、Web 30/30、production build、Ruff、mypy 94 files PASS。三档生产只读 Playwright 均零溢出/浏览器错误/失败响应/公网/原片/包写入，无截图，证据更新于 `docs/evidence/g10/project-package-uat-2026-08-15.json`。导入后的媒体注册、缩略图重建尚未完成，FR-PRJ-002 仍 PARTIAL。

> 2026-08-15 FR-PRJ-002 package foundation：新增受控 v2 项目包导出与 dry-run API/UI。导出冻结 `localdrama.project-state.v2`（项目、季/集/场/镜头当前结构、ProductionPlan/Profile binding/DeliveryTarget 引用），收集项目文件并生成 `localdrama.project-package.v2` manifest，逐文件 SHA-256/size、导出前后源复验、partial+原子发布、确定性复用；排除 Jobs/Attempts/Reviews/Audit/Outbox/cache/work。dry-run 仅接受项目 `exports/project-packages` 内注册包，流式校验 zip-slip、重复路径、schema、manifest、hash/size、entry/展开上限、压缩比、磁盘与 identity 冲突，明确 `REBIND_EXISTING`/`IMPORT_AS_COPY_REWRITE_IDENTITY`，不执行导入。隔离真实 Unicode/空格文件导出、复用、API 与恶意 ZIP/越界负例通过。生产三档只读 UI 验收未点击导出，零包写入/溢出/浏览器错误/失败响应/公网/原片请求，41px 按钮，无截图。完整回归 API 142 passed / 4 Comfy live deselected、Web 29/29、build/Ruff/mypy 93 files PASS；证据 `docs/evidence/g10/project-package-uat-2026-08-15.json`。外部 staging/import commit/identity 重写/断点重试/注册与缩略图阶段未完成，FR-PRJ-002 仍 PARTIAL。

> 2026-08-15 FR-PRJ-001 creation-plan continuation：新增只读 `POST /projects:plan` 与四步新建向导。计划端点复用项目 code/spec 领域校验，检查数据库身份冲突、孤立目录与真实卷可用空间，60 集预估，返回配置 blockers，且 `mutated/runtime_contacted/network_contacted=false`。UI 的画幅/fps/时长均无默认值，必须明确勾选“稍后配置并接受阻塞”才能预检，最终按钮前零写入。生产三档 Playwright 走到 `READY_WITH_CONFIGURATION_BLOCKERS` 后停止，未创建项目；零溢出/console/page error/失败响应/公网/原片请求，最小控件 40px，无截图。完整回归 API 139 passed / 4 Comfy live deselected、Web 28/28、build/Ruff/mypy 92 files PASS，证据 `docs/evidence/g10/project-create-wizard-uat-2026-08-15.json`。因季数、制作分辨率、语言/字幕及创建时直接绑定路线尚未齐，FR-PRJ-001 仍诚实记 PARTIAL。

> 2026-08-15 FR-PRJ-001 truthfulness fix：`POST /projects` 不再根据请求标志伪造空 blockers；新项目创建后立即从持久化绑定表投影真实 `PROFILE_NOT_BOUND`、`PRODUCTION_PLAN_NOT_BOUND`、`DELIVERY_TARGET_NOT_BOUND`。`ConfigurationService.blockers` 同时修复未知项目错误返回三项 blocker 的 fail-open 行为，现明确 `PROJECT_NOT_FOUND`。项目创建的目录/数据库回滚既有保证不变；完整创建向导与创建时原子绑定仍待继续，故 FR-PRJ-001 只记 PARTIAL。

> 2026-08-15 FR-PRJ-006 continuation：新增“复制为新剧模板”服务/API/UI。副本使用全新 Project/Season/Episode/Shot/revision UUID 并重置 DRAFT，只复制季/集/镜头结构、当前镜头结构字段（解冻为 revision 1）、ProductionPlan、本地 DeliveryTarget 与仍为 Published 的 ACTIVE Profile 绑定；明确排除媒体、工作区资产授权、BrandKit、Job、审核、交付与审计历史。文件树与 SQLite 使用同一失败边界，模拟中途失败后数据库/目录均无半成品；重码与孤立目录冲突不会覆盖或删除已有内容。生产 UI 只读展开验收未确认复制，1440×900、1280×800、1024×768 零溢出/console/page error/失败响应/公网/原片请求，40px 控件，未截图。完整回归 API 137 passed / 4 Comfy live deselected、Web 27/27、build/Ruff/mypy 92 files PASS；证据 `docs/evidence/g10/project-template-copy-uat-2026-08-15.json`。G7 唯一许可证证据阻塞及后续有序门禁状态不变。

> 2026-08-15 FR-PRJ-003 continuation：补齐项目列表服务端标题/code 搜索与状态筛选，SQL `%/_` 作为字面值安全转义、非法状态 422；概览和生产台新增 40px 可用筛选控件。既有归档继续是审计状态转换，不删除文件且活动 Job 硬阻塞。生产只读搜索 `g2_smoke2 + DRAFT` 精确返回一项，1024×768 零溢出/浏览器错误/失败响应/原片请求；未归档生产项目。全量 API 134 passed / 4 Comfy live deselected、Web 26/26、build/Ruff/mypy 92 files PASS。证据 `docs/evidence/g10/project-list-filter-uat-2026-08-15.json`。门禁状态不变。

> 2026-08-15 FR-TML-004 continuation：新增冻结 TimelineRevision 的 OTIO + CMX 3600 EDL 本地导出。OTIO 使用 `Timeline.1`/`Clip.2`、相对项目媒体 URL 和冻结 MediaVersion ID/SHA/size；EDL 使用项目显式 fps 生成 source/record timecode。产物先写 partial，再带 manifest/hash 原子发布；相同输入逐文件复验后复用，源或产物篡改拒绝，失败不修改 SQLite/TimelineRevision。生产 v1 的 3 个真实视频 item 已导出到 `timeline-v1-a3cd25d01d62`，三视口零溢出、零 console error/warning、零失败响应、零原片请求。全量 API 132 passed / 4 Comfy live deselected、Web 25/25、build/Ruff/mypy 92 files PASS。回归同时修复 `scripts/check.ps1` 忽略原生命令非零退出码的假绿缺陷，pytest/mypy/Ruff/build/test 现均显式 fail-fast。证据 `docs/evidence/g10/timeline-otio-edl-export-uat-2026-08-15.json`。G7 与有序退出状态不变。

> 2026-08-15 FR-IMG-007 continuation：新增分集“已选媒体联系表”真实导出服务/API/UI，只跟随 `media_assets.selected_version_id` 当前权威选择；导出前后校验源文件、复制原件和 320px WebP 的大小/SHA，自包含 HTML + manifest 写入项目 `01_story/.../exports`，不写 SQLite、不接触 runtime/network。相同输入会完整复验既有目录后幂等复用，源或导出篡改均硬拒绝。全量 API 129 passed / 4 Comfy live deselected、Web 23/23、production build、Ruff、mypy 91 files PASS；生产项目真实导出 1 项并在 1440×900、1280×800、1024×768 零溢出、零 console error/warning、零失败响应、零原片请求。证据 `docs/evidence/g10/contact-sheet-export-uat-2026-08-15.json`。G7 仍因真实模型 license evidence 阻塞，不改变后续门禁状态。

> 2026-08-15 continuation: React review/jobs/Profile/canvas/status domains have been split from the app shell (`81e8b23`, `ba97a0e`, `15154b3`); `App.tsx` is now 289 lines. A selected-episode projection bug was fixed so deep-linked production queries and the visible title use the same record, including a 60-episode/episode-47 regression. The remaining icon-system P1 was closed with local, decorative, non-focusable 1.75px outline SVGs and no structural font glyphs. Web 21/21, production build and nine real-browser checks across 1440×900, 1280×800 and 1024×768 pass. Offline SBOM remains DRAFT but now distinguishes target applicability from the complete lock inventory: 317 packages, 244 with concluded/declared license, 73 `NOASSERTION` entries all proven by pnpm `os`/`cpu` constraints to be uninstalled non-Windows-x64 optional packages, target-runtime `NOASSERTION=0`. Full API check is 125 passed / 4 Comfy live deselected, Ruff clean, mypy clean across 90 files. Release audit remains honestly `IN_PROGRESS`: G7 is 11/12 and blocked only by real MiniMax-H3 model license evidence; ordered G8/G9 and FINAL release artifacts remain blocked. No public network, production mutation or ComfyUI interaction occurred.

> 最新续跑（2026-08-14）：用户已授权自动审核与继续开发，并允许使用 ComfyUI；当前仅确认 127.0.0.1:8188 有监听，未提交/轮询/恢复任何任务。G7 readiness 通过 11/12，唯一失败仍为 `MODEL_LICENSE_HASH_QUANTIZATION_REPORT`；本地 safetensors header 的 `__metadata__` 为空，未发现可替代操作者许可证记录的证据。

> 2026-08-14 continuation: G7-03/G7-07 adapter contract layer completed. `GET /api/v1/adapters/contracts` exposes a read-only LOCAL_ONLY registry for Comfy loopback, the real Local LLM loopback (Ollama API), Local CLI, and FFmpeg/FFprobe. Remote/public endpoints, URL credentials, and remote executable references are rejected before runtime access; contract inspection makes no socket/process/model call. Added ADR-0013 and `test_adapter_contracts.py`. Full mypy is clean (83 files). G7 remains `IN_PROGRESS`; the only hard blocker is real model license evidence.

> 2026-08-14 continuation: added read-only `GET /api/v1/projects/{id}/model-compatibility` and the Profiles/Diagnostics model evidence panel. It projects persisted artifact/report/hash/quantization/license state without hashing, runtime contact, network access, or mutation. Production truth: 10 model artifacts, 1 offline report, 0 PASS, 1 BLOCKED report (H3 video VAE SHA `5a624684...ceb148`, 5,207,806,104 bytes, 560 tensors/F16, `HEADER_MATCHED`), and 10 missing project-local license evidence records. No plugin LICENSE/NOTICE, download URL, or inferred term was promoted to model license evidence. Focused model tests pass; full safe regression is `116 passed / 4 live deselected`, Web `11/11`, build, Ruff, mypy 88 files; three viewport checks are overflow-free with zero browser console warnings/errors. ComfyUI Desktop remains loopback-only at `127.0.0.1:8188`, CUDA `2.10.0+cu130`, 39 H3 nodes, Manager UI disabled/offline, no public established connections. G7 remains honestly `IN_PROGRESS` with `MODEL_LICENSE_HASH_QUANTIZATION_REPORT` as the only false check.

> 2026-08-14 G9 continuation: canvas search/upstream/downstream focus and a read-only SQLite capacity snapshot were added. `/api/v1/capacity/snapshot` reports observed queue/worker/GPU state only (`OBSERVED_NOT_BENCHMARKED`), never creates or claims jobs, and explicitly reports webhook `NOT_IMPLEMENTED`. Three viewport canvas UAT remains clean; G9 remains progress and G7 license evidence remains the first hard gate.

> 本轮继续：新增不可逆 migration `0020_g7_model_license_evidence` 与项目内 license evidence 导入 API。它只接受操作者真实提供的项目内 JSON，强制校验模型当前 SHA-256、license 名称、路径边界和 symlink；没有真实记录则永远 BLOCKED。生产迁移已在线备份并完成 `0019→0020`，`integrity_check=ok`。安全 API `100 passed / 4 live deselected`、Web `9/9`、build、Ruff、mypy 全绿。Profile Editor Playwright 已修复遗留 DRAFT 竞态：1440×900 从 Published v12 派生新 DRAFT v15 后只验证一次，1280×800/1024×768 只读，三档零溢出/零浏览器错误/零失败响应/零原片请求；仅读取 <=720px WebP，视觉证据在 `docs/evidence/g7/profile-editor-visual-review-2026-08-14.json`。G7 仍 `IN_PROGRESS`，唯一首阻塞是现实的模型 license evidence 缺失；不伪造 PASS。ComfyUI 8188 保持关闭。

> 随后完成 G7-05/G7-06 非 ComfyUI 工作：新增只读 `GET /api/v1/projects/{id}/configuration` 与工作台“项目配置快照与切换影响”面板，显示真实 ProductionPlanVersion、三项 Profile binding、DeliveryTargetVersion、冻结 Job/DeliveryPackage 数量；`runtime_contacted=false`、`network_contacted=false`、`mutated=false`，明确 Profile 切换不漂移旧 Job、REMOTE transport 禁用。API 回归升至 `102 passed / 4 live deselected`；Web 9/9、build、Ruff、mypy 全绿；三档 Playwright 仍零溢出/零浏览器错误/零失败响应/零原片请求，低码率缩略图自动视觉 PASS。当前唯一 G7 blocker 仍为真实模型 license evidence 缺失。

> 续跑结果：G7-09 capability compatibility 已由 Published v12 + matching PASS attestation 闭环；G7-08 已用真实 loopback Comfy/LLM/diagnostics 客户端完成零公网全链路测试，attestation `51b05922-fce9-496e-bb9e-a627ba346f77`；G7-10 工作区资产授权与 BrandKit 已闭环（授权 `853fd791-0bc8-4cf5-bf12-fc54c3542caa`、BrandKit v1 `5a0d3a95-1aea-4482-a137-2a85439c8373`）。当前 readiness 首阻塞为 `MODEL_LICENSE_HASH_QUANTIZATION_REPORT`：离线 H3 video VAE 报告 `d586967e-0634-4c8d-8887-193d398beda9` 完成完整 SHA/header/FP16 对照但因缺少真实 license evidence 保持 `BLOCKED`，禁止伪造 PASS。迁移已到 `0019_g7_model_compatibility_reports`；安全 API 97 passed/4 deselected、Web 9/9、build、Ruff、mypy 81 files、三档 Profile Editor Playwright 全绿。ComfyUI 保持关闭。

> 2026-08-14 G6 正式退出：批准关键帧生成的 4 条真实 I2V proxy（seeds 260826—260829）均 SUCCEEDED、晋升独立 MediaVersion 且 machine QC PASS；人工选择第四条 `2b5e49e4-09e0-4dc0-b208-29b33670589f` 为 winner。由 winner 仅切换到 Published formal Profile 后生成正式 MediaVersion `bf6f2151-a104-48d4-9c85-b89a7bad68c9`（H.264 High、512×928、24fps、4.458008s、107 帧，SHA `68cbc0b0...`），machine check `4a04f522...` PASS，人工 ReviewDecision `c1b3cf70...` APPROVED。只读 G6 readiness 七项全部 PASS。人工视觉只使用 720×328、最大 18KB WebP；冻结提示与现代室内源图的语义不一致已在审核中披露，不宣称错误语义目标实现。完整安全回归为 API 79 passed / 4 live deselected、Web 4/4、production build、Ruff、mypy 全 PASS。G6 exit report 已建立；现在只允许按顺序进入 G7，G8/G9 仍为 progress，长期目标未完成。Figma 今日 inspect 仍被 Starter MCP quota 拒绝，未写画布或重复 foundations。

> 2026-08-14 续跑：新增只读 `GET /api/v1/projects/{project_id}/gates/g8?episode_id=...` 与分集生产页 G8 Formal Exit Readiness 面板。该投影严格读取真实 timeline/audio/subtitle/render/review/delivery 表，不创建媒体、Job、render 或 delivery，不接触 ComfyUI/网络，也不以机器状态替代整集批准。生产 `EPISODE_001` 当前真实结果为 0 个 timeline SHOT-owned VIDEO、0 个授权音轨、0 个字幕 revision、无整集 render/批准/delivery，首阻塞 `THREE_REAL_SHOTS`；证据见 `docs/evidence/g8/g8-readiness-readonly-2026-08-14.json`。API G8 回归、Ruff、mypy、production build 通过；1440×900、1280×800、1024×768 均零溢出/零 console 错误。G7 仍唯一首阻塞 `MODEL_LICENSE_HASH_QUANTIZATION_REPORT`，G8 仍 IN_PROGRESS，禁止越级宣告。

> 同轮新增只读 `GET /api/v1/projects/{project_id}/gates/g9?episode_id=...` 与业务画布 G9 Formal Exit Readiness 面板。生产项目真实观测为 1 个镜头、5 个可见节点、1 个历史 preflight；懒加载/300 上限、布局与业务依赖隔离、preflight 持久化三项通过，正式 100—300 节点性能 UAT 与完整三视图可访问性 checklist 仍未通过。62 镜头/100 节点自动化 fixture 仅是测试证据，不冒充生产退出；证据见 `docs/evidence/g9/g9-readiness-readonly-2026-08-14.json`。新鲜浏览器 tab 三档验收零溢出、零 console 错误/警告，ComfyUI 仅保持本地回环监听，未提交任务。

> G10 发布准备新增只读 `scripts/release_audit.py` 与草稿工件 `docs/release/install-upgrade-rollback.md`、`docs/release/sbom.json`、`docs/release/go-no-go.md`。审计实测当前 SQLite integrity=ok、migration head=`0020_g7_model_license_evidence`、最近 5 份备份 integrity=ok；顺序 G7/G8/G9 均为 `IN_PROGRESS`，草稿工件均非 FINAL，因此 release audit 真实状态为 `IN_PROGRESS/NO-GO`，证据见 `docs/evidence/g10/release-readiness-2026-08-14.json`。这些文件不能替代最终安装/升级/回滚演练、完整 SBOM 或 go/no-go 签字。

> G7 顺序启动：新增只读 `/projects/{id}/gates/g7`，不探测 runtime、不访问网络、不修改状态；并用公网 URL 伪装 LOOPBACK 的负例证明会在访问前硬拒绝。生产项目已显式绑定 ProductionPlanVersion `5a0a2a07-8703-4642-9df5-3bd33ad41970`、Published I2V/T2V/本地 LLM Profile 三项矩阵，以及 LOCAL_FILESYSTEM DeliveryTargetVersion `f001acd0-096f-44f1-b466-e0f11212a090`。G7 仍为 `IN_PROGRESS`，当前首阻塞 `PROFILE_EDITOR_TEST_PUBLISH`，后续 compatibility、零公网全链路、工作区资产授权及 license/hash/量化报告亦保持硬阻塞；未以基础绑定冒充 G7 退出。新增回归后安全 API 82 passed / 4 live deselected。

> 23:11 真实 I2V 续跑：用户释放 ComfyUI 后，以 Production sandbox/localhost/H3-only/ephemeral 策略执行批准帧 `0d389e44-0fc3-47e2-b492-f7e3501ccf0c`。高质量 768×1344/50-sigma 探针 Job `c2dd8ab0-fc37-43f3-9413-be1b55f99d9e` 被节点错误的 75 秒 telemetry 阈值中止（FAILED）；修复 FL2VA 慢路径豁免后，重试 `fd4e3e58-1276-4b09-99c5-5ca85c782899` 健康运行但成本过高，受控取消为 CANCELLED。随后创建并以服务端 attestation 发布 proxy-evidence workflow `6e09d8c3-bb15-4c48-9253-cbdf609a8670`（352×640、4 秒、21 sigma）；首次 Job `0cf08a33-0307-4f53-8fda-67251a38757b` 完成采样但被固定 18GB 解码门槛拒绝（FAILED）。将门槛按输出面积缩放为 8—18GB 后，新 Job `2b8190de-cfe8-4dbc-87ea-cdaf743d8176` / Attempt `29f68cda-67e5-4f02-962b-b1946ef708bf` / prompt `7a3e259d-1c61-40f1-a1de-960b813509a5` 真实 SUCCEEDED：20/20 DiT、总时长 222.10s，VERIFIED artifact `690c65df-f329-4e40-b014-553f39246be6`（562,600 bytes，SHA `0219a3dd...`），FFprobe H.264 352×640 24fps 4.458s/107 帧。晋升 MediaVersion `dc26b0ad-7341-43f9-aa5e-9c9ba97e9664`，machine QC PASS；I2V Profile 新不可变 v2 `050c5caf-f47d-4641-8ec3-893e663efdc9` 已以完整 workflow/job/attempt/artifact/media/approved-FIRST_FRAME 谱系发布。G6 前两项 PASS，下一项 `FOUR_REAL_PROXY_TAKES (0)`；仍非 G6 PASS。Worker 已停止，显存恢复约 23.7GB。安全 API 79 passed / 4 live deselected，Web 4/4、build、Ruff、mypy PASS。

> 22:06 续跑状态：用户重新占用 ComfyUI，当前明确禁止提交、轮询、重启、恢复或抢占。已在不访问 8188 的前提下完成关键帧真实批准后的控制面加固：正式 `I2V_PROXY`/`I2V_FORMAL` 输入只接受同项目、同镜头、当前已批准且有非 stale APPROVED ReviewDecision 的 KEYFRAME，并把 `approval_id` 冻结到 Variant binding、Job snapshot、recipe hash；重复批准改为幂等返回，既有 9 条批准审计历史保留不删除。新增只读 `i2v-probe-plan`，生产项目返回 READY，冻结批准 `c092a9f5-20fc-4165-bd4b-19ef8487d7cc`、工作流 `4670a8f1-1c37-4a75-a9bc-7663818d7135` 与计划哈希；调用前后 Job 均为 16，`would_create_job=false`、`would_contact_comfyui=false`。工作台已显示该证据与安全声明。G6-07 新增只读 workflow history API/UI，生产读取 5 个不可变版本、4 个 PUBLISHED，明确 `runtime_contacted=false`；不会代替操作者验证、发布或回滚。安全 API 78 passed / 4 Comfy live deselected，Web 4/4，生产构建、全 API Ruff、mypy 75 source files PASS（8 个历史 Alembic import-order 告警已仅做排序修复，迁移测试 2/2 PASS）。1440×900、1280×800、1024×768 Playwright 均 7 项门禁、零水平溢出、零 console warning/error、全部媒体 `size=small`；视觉仅检查 720px、11—13KB WebP。API 当前 PID 23724，HEALTHY/LOCAL_ONLY。G6 仍为 IN_PROGRESS，下一门禁仍是发布真实 I2V Profile；不得进入 G7。

> 最新续跑状态：用户已明确释放 ComfyUI。4/4 live 控制面测试通过；当前安全 API 为 69 passed / 4 ComfyUI live deselected。两次真实 H3 崩溃均按持久队列恢复为 `NEEDS_ATTENTION`/`ORPHANED`；根因定位为解码退出阶段 VAE CPU offload 的 Windows `c10.dll` access violation。显式 ephemeral single-job 策略修复后，第三条探针 Job `48b87103-0729-4935-a6bf-1a1d5d2c14ab` 已真实 `SUCCEEDED`，形成 VERIFIED H.264 artifact。Production Comfy 保持独立 sandbox/PID/log、H3-only whitelist 与启动日志零公网命中，成功后已停止并回收显存。G6 仍非 PASS：人工 winner、正式视频和正式审核尚未完成。
>
> 随后新增 `0013_g6_artifact_media_lineage` 和 artifact→MediaVersion 晋升 API；真实 artifact 已晋升为 MediaVersion `1f74dbd3-c2f9-4897-acac-925d3ceb0e0a`，保留 Artifact/Attempt 谱系，FFprobe 与 machine check PASS，并进入 review inbox。安全 API 当前 69 passed / 4 deselected、Web 3/3、生产构建 PASS、mypy 72 source files PASS。未做人工批准/选择；G6 仍非 PASS。
>
> 四条正式 proxy 随后以独立 Variant/Job、seed 260821—260824 串行真实生成并全部 SUCCEEDED；四个 artifact 均晋升为独立 PROXY MediaVersion，FFprobe/machine check PASS，完成进程和显存回收。640×368 低码率联系表显示整体偏暗，因此没有代替人工勾选 identity/motion/continuity，也没有选择 winner。G6 仍非 PASS。

> 生成工作台候选架已接入真实 review inbox，并修复初始 `sourceVideoId` 为空时产生的 `/media-versions//thumbnail` 竞态请求。全新 Chromium 会话在 1440×900、1280×800、1024×768 均为零水平溢出、零 console error、零 page error、零失败响应；候选分别按 4/4/2 列响应，证据只查看最大边 720px 的低码率 JPG。结构化结果位于 `apps/web/output/playwright/generation-validation.json`。

> 续跑后新增 G6 七项只读 readiness API/UI，生产项目真实首阻塞为 `APPROVED_KEYFRAME`（0 张已批准图片），不会自动选择或审核。H3 FL2VA I2V workflow 与隔离 input-root 媒体物化已实现；WorkflowVersion `4670a8f1-1c37-4a75-a9bc-7663818d7135` 经受控 Comfy 节点/runtime layout 验证 PASS 并发布。因尚无真实 I2V 成功媒体谱系，I2V Profile 仍未发布。受控 Comfy 3/3 live 测试通过后已停止。当前安全 API 72 passed / 4 deselected、Web 3/3、build/Ruff/mypy 74 source files PASS；三档 Playwright 对新增门禁仍为零溢出与零错误。
>
> 最新续跑实现真实 FrameAnchor 到 shot-owned KEYFRAME candidate 的幂等派生与 UI 人工审核入口，并修复 Shot 项目归属查询；该入口不产生 selection/approval。I2V Profile 证据发布现必须使用匹配的 FL2VA workflow、完整成功谱系和同项目已批准 FIRST_FRAME，未批准图片即使生成视频成功也拒绝发布。Comfy 输入复制后复算 SHA-256。最终安全 API 75 passed / 4 Comfy live deselected、Web 4/4、build/Ruff/mypy PASS；三档 Playwright 均 7 项门禁/6 个 small 候选/零溢出/零错误。Figma inspect 被 Starter quota 拒绝，未写画布、未重复 foundations。
>
> 生产 UAT 已创建待审核 KEYFRAME candidate `0d389e44-0fc3-47e2-b492-f7e3501ccf0c`（来自现有 VERIFIED Last FrameAnchor，镜头 `020f9248-14b7-4f92-9edd-ee587ffdedf3`），机器 QC PASS。审核 UI 只请求 320px small 缩略图，显示机器 PASS 和“选择为 KEYFRAME”，但没有 selection/review/approval；G6 仍真实为 `IN_PROGRESS / APPROVED_KEYFRAME (0)`。生产 API 当前 PID 24604，`HEALTHY / LOCAL_ONLY`；Comfy 本轮未启动、未提交、未轮询。

## 1. 接管目标

继续执行既有长期目标：严格依据 `F:\AI_Projects\h3\LocalDramaStudio_Blueprint_v2` 完整实现 `F:\AI_Projects\h3\local_drama_studio`，按文档 09 的 G0→G10 顺序推进，直到 86/86 功能需求、14/14 非功能需求、85 个主干测试、完整本地一条龙 UAT、无 P0/P1 缺陷及最终发布证据全部通过后，才能宣告完成。

当前目标尚未完成，不得因为已有页面、部分测试或部分阶段报告而标记完成。

## 2. 不可变约束

- 开始/继续前以蓝图 `00` 的权威顺序处理冲突，文档范围为 00—10、12—17。
- `11` 是延期的 legacy 迁移专项，本次禁止实施；不得进入 G11。
- 首版必须保持 `LOCAL_ONLY`，运行期零公网请求。
- 不实现云 Provider、API Key、计费、多租户。
- 不允许 Mock、静态页面或伪造证据冒充真实能力完成。
- migration、SQLite 权威状态、真实 API、真实本地模型、异常恢复、安全、性能和 E2E 均不可跳过。
- H3 只是可配置候选 Profile；严格服从 `model_manifest.json`、禁用模型和“一任务一 Worker”。
- 媒体浏览与视觉验收只查看缩略图。对用户展示/人工查看的截图必须压缩到不超过 720px、JPEG/WebP 质量约 50—60；审核 UI 默认只请求 `size=small` 缩略图，不自动加载原片。
- 用户最后关于 ComfyUI 的有效指令是“我先用一会，用完会告诉你”。在用户明确通知用完之前，禁止向 ComfyUI 提交任务、轮询任务、重启、恢复或抢占显存。
- 普通工程问题自行调查和修复；只有文档无法推导的重大产品冲突、不可逆破坏或新增权限才询问用户。

## 3. 阶段状态（不得夸大）

- G0—G5：已有退出报告和证据，见 `docs/phase_reports/G0_exit_report.md` 至 `G5_exit_report.md`。
- G6：PASS（2026-08-14）。见 `docs/phase_reports/G6_exit_report.md`；旧 `G6_progress_report.md` 保留为过程证据。
- G7：现按顺序进入，尚未 PASS。
- G8/G9：已有代码与进度证据，但属于 progress，不代表可以跳过前置门禁。见对应 progress report。
- G10：未完成。
- 最终 86/86、14/14、85 主干测试和完整 UAT：未完成。

## 4. 已实现的核心工程

- FastAPI + SQLite WAL 本地后端、Alembic migrations、OpenAPI 快照和生成 TS 客户端。
- 项目/季/集/镜头生产 read model、媒体导入/探测/hash/Range/thumbnail/filmstrip/waveform。
- 审核模板、selection 与 approval 分离、stale revision、防并发覆盖、批量审核基础契约。
- SQLite 持久任务队列、attempt/lease/heartbeat/reconcile/outbox/SSE、独立本地 Worker。
- 本地 LLM 状态与真实剧本草稿链路；此前 DeepSeek 本地草稿成功。
- H3 candidate runtime/Profile/工作流基础；真实 H3 长任务曾完成 denoise 后在视频/音频 VAE 阶段进程退出，因此 G6 不能宣告通过。
- `0009_g6_continuity_stale` 已加入连续性 revision/stale 字段；批准替换 Shot 首帧前可预览 plan hash，并在同一事务中使 REQUIRED `END_AT_NEXT_FIRST` 及绑定旧首帧的下游 Variant stale，同时写 audit/outbox。普通实验选择不传播。
- `0010_g6_frame_anchor_stale` 补齐上一镜头视频 winner 替换链路：旧视频派生的 `LAST_FRAME` Anchor、引用它的 REQUIRED Transition 及绑定 extracted frame 的下游 Variant 同事务 stale；普通 `PROXY_WINNER` 选择仍不传播。
- TC-VAR-015 已补真实文件完整性门禁：Variant preflight 与 FrameAnchor extraction 会重新流式计算受控项目文件的 SHA-256/size，不再只信任数据库 `VERIFIED` 标签；篡改时持久标记 `CORRUPT` 并在 Variant/Job/Anchor/派生媒体产生前阻塞。
- 已存在 FrameAnchor 的 Transition validate 也会实时复核 source 与 extracted 两端文件；任一端在 Anchor 建立后被篡改，Transition 明确 `BLOCKED` 并指出具体 media role/id，不能依赖旧完整性状态继续生产。
- stale 状态现已在消费端强制执行：stale Variant 不能作为 derive parent，stale Anchor 的 extracted media 不能进入新 Variant preflight，stale Anchor 不能创建新 Transition；三类拒绝均在持久化前发生，历史对象仍可只读追溯。
- Variant create 会在使用已确认 plan hash 前重新执行实时文件完整性 preflight；测试在 plan 成功后、create 前篡改输入，旧 plan hash 仍被 `SOURCE_INTEGRITY_FAILED` 阻塞，零 Variant/Job/创建审计。
- TC-VAR-013 新增 `PROFILE_BRANCH` derive-plan：目标必须是不同且 Published 的 ProfileVersion，计划只改变 `profile_version_id`，Prompt/参数/seed/bindings 保持不变；Candidate 与混合字段覆盖均在落库前拒绝，Variant/Job 计数保持不变。真实双 Profile Job/artifact/A-B review 尚未执行，因此只算离线前置契约进展。
- ComfyUI 测试隔离已从易错的手写 deselect 改为正式机制：4 个真实 loopback 测试标记 `comfyui`；`pnpm api:test:safe` 固定执行 `-m 'not comfyui'`，并设置 `LOCAL_DRAMA_COMFY_ACCESS=disabled`。`ComfyClient` 会在网络请求前 fail-fast，diagnostics 会直接返回 `access_disabled`；两条路径均有不调用网络的回归测试。默认 `scripts/check.ps1` 使用安全套件，仅显式 `-IncludeComfyUI` 才运行 live tests。
- TC-VAR-004 的 Provider random 离线快照契约已实现：`RESUBMIT_PROVIDER_RANDOM` 只允许 Published Profile seed support=`NONE/OPTIONAL`，强制 `seed_policy=PROVIDER_RANDOM`、`explicit_seed=null`，并用迁移 `0011_g6_provider_random_nonce` 冻结每次唯一 UUID nonce。nonce 进入 recipe/plan hash，响应明确 `NON_REPRODUCIBLE` 与“不保证重复结果”；直接 create 仍重复执行 Profile/字段范围/nonce 门禁。当前不创建 Job，避免在语义输入尚未安全 sandbox 编译时制造可被 Worker 抢占的假任务，因此 TC-VAR-004 整体仍 PARTIAL。
- Variant commit-time 分支不变量已补齐：直接构造 plan 绕过 derive API 时，`RESAMPLE_NEW_SEED` 只能改变 explicit seed，`PROFILE_BRANCH` 只能改变 ProfileVersion，`RESUBMIT_PROVIDER_RANDOM` 只能改变 seed policy/seed/nonce；父对象、seed 支持、unchanged 值和 nonce 均复核。测试覆盖 preflight 与实际 `create_variant` 入口，拒绝后 Variant/Job 计数不变。
- TC-VAR-009 后端/API 已用真实本地三帧视频收口：迁移 `0012_g6_frame_anchor_resolution` 冻结 requested/resolved time、真实 frame index、source SHA 与 `FFPROBE_PTS_FRAME_INDEX` 方法。服务按 FFprobe 的真实 PTS（兼容 VFR）解析播放头到不晚于请求时刻的帧，再按 frame index 精确提取；支持 FIRST/LAST 原生模式，时间/索引/模式严格互斥，越界在媒体注册前零副作用拒绝，临时 PNG 必清。播放器“用作”菜单仍是 FR-GEN-007 的 UI 未完成项。
- TC-VAR-007 已固定为可行动的 Profile capability 错误：不支持 END_FRAME 时返回当前 ProfileVersion、unsupported/supported roles、`FIRST_LAST_KEYFRAMES` capability 与配置/发布新 ProfileVersion 的 suggested action，零 Variant/Job。
- TC-VAR-003 已用真实 SQLite 持久队列和 `CPU` channel 验证 operational retry：同一 Job 从 attempt 1 FAILED 经显式 retry 产生 attempt 2 SUCCEEDED，Variant=1、Job=1、源 take=1；未写入或 claim `GPU_H3`。
- 业务画布、布局保存和运行预检；时间线/交付等后续模块已有部分代码和进度证据，必须依据追踪矩阵复核，不得仅按文件存在判定完成。
- 浏览器前端为 React 19 + Vite + TypeScript + React Query + XYFlow。

## 5. 本窗口完成的前端改造

设计方向已确定为“混合专业制作工作站”：深色持久 chrome/媒体区、暖中性生产工作区、克制朱红创作强调；拒绝 OTT/营销页式粉色大 Hero、玻璃拟态和无意义发光。

权威文件：

- `design-system/localdramastudio/MASTER.md`
- `docs/ui-ux-audit-2026-08-12.md`

已落地：

- 生成工作台拆到 `apps/web/src/features/generation/GenerationWorkbench.tsx`。
- `view/project/episode/shot` 写入 URL，可刷新恢复并支持浏览器前进/后退。
- 顶部显示真实项目、分集选择；生成前绑定真实镜头。
- 系统 KPI 从创作页大卡片收敛为紧凑状态条。
- Profile 按真实 capability/status 显示；未发布能力保持阻塞，不静默回退。
- 审核中心列表和详情只请求合法 IMAGE/VIDEO 的 `thumbnail?size=small&frame=poster`；其他媒体显示类型占位，不失败探测原片。
- Playwright 网络验收曾确认原片 `/content` 和 `/filmstrip` 请求为 0。
- 审核检查项可选择 PASS/FAIL，审核决定可选 APPROVED/NEEDS_CHANGES/REJECTED。
- `review-context` 新增权威 `subject_revision`，前端接入真实 `submitReview` API，保留 stale 并发保护。
- OpenAPI 与 `apps/web/src/generated/api.ts` 已同步生成。
- 本窗口最后一次完整验证：G4 API 2/2、前端 2/2、TypeScript、Vite production build 均通过。

注意：另一个 Figma 窗口随后又修改了相同 React/CSS 文件，当前最终验证应以它正在执行的最新结果为准，不能只引用上述旧结果。

## 6. Figma 窗口实时状态

正在运行的任务：

- thread id：`019ff6a1-e711-7fd2-8929-20c7110291ba`
- Figma 文件：`LocalDramaStudio Product Design`
- file key：`ZtpUS9N1Of3vRYIFQDSJqh`
- URL：https://www.figma.com/design/ZtpUS9N1Of3vRYIFQDSJqh

已确认真实完成：

- Figma `whoami` 和连接成功。
- 新 Design 文件创建成功。
- 已创建 73 个 token/variables、9 个 styles、3 页结构和两套基础画布。
- 已建立 primitives、semantic colors、dimensions/radii、typography foundations。
- 已写入本地状态账本：`design-system/localdramastudio/figma-state.json`。

未完成/风险：

- Starter/View 席位的 Figma MCP 调用额度已触顶，后续透明容器修复调用失败。
- 组件库和完整“生成工作台”精修画布尚未完成，不得声称完成。
- `figma-state.json` 当前只记录早期 foundation 状态，落后于画布真实进度；恢复后先通过 Figma metadata/variables 检查，再更新账本，避免重复创建。
- Figma 基础画布视觉验收发现部分 auto-layout frame 默认白色填充，计划清除的节点修复尚未因额度限制落地。

Figma 窗口在额度受限后继续直接重构 React，已经改动：

- `apps/web/src/app/App.tsx`：skip link、主内容 focus target、brand mark 等。
- `apps/web/src/features/generation/GenerationWorkbench.tsx`：`aria-current`、`aria-pressed`、媒体槽说明、预检摘要等。
- `apps/web/src/styles.css`：全面语义 token 和响应式结构重构，取消 `body min-width:1024px`，准备 1024 紧凑横向导航。
- 2026-08-13 当前轮构建、测试及 1440×900、1280×800、1024×768 Playwright 验收已完成：前端 3/3、生产构建 PASS、安全 API 64 passed / 4 ComfyUI live deselected。
- 生成工作台已接入真实 FrameAnchor：当前项目 VIDEO 可选择“用作首帧/当前帧/末帧”，只请求 small 缩略图；真实浏览器末帧请求返回 201，冻结第 106 帧、4.417 秒、SHA 与 `FFPROBE_PTS_FRAME_INDEX`，并只填入未提交草稿，不宣称已创建 Variant。
- 验收暴露运行库停在 Alembic `0007`，首次加载新接口曾分别出现旧进程 422 和未迁移库 500；迁移到 `0012` 后重试 201。`scripts/start.ps1` 现强制 `alembic upgrade head` 成功后才启动 API，防止代码/Schema 漂移。
- 视觉缩略证据位于 `output/playwright/frame-anchor-acceptance/acceptance-{1440x900,1280x800,1024x768}-thumb.webp`（宽 720px、低码率）；原始视口截图已就地降采样，不保留/展示大图。
- 修复后的全新 Playwright 会话在 1024×768 下为 0 console errors / 0 warnings，所有业务请求均为 `127.0.0.1`，且不再发出无项目过滤条件的 review inbox 请求。

## 7. 接管时的并发规则

另一个 Figma 窗口正在同一个实际目录 `F:\AI_Projects\h3\local_drama_studio` 写代码，不是隔离 worktree。接管者必须：

1. 先等待 thread `019ff6a1-e711-7fd2-8929-20c7110291ba` 当前回合完成并读取它的最终报告。
2. 在它完成前不要同时编辑 `App.tsx`、`GenerationWorkbench.tsx`、`styles.css`、`figma-state.json`。
3. 完成后重新读取磁盘现状，不依赖旧上下文中的 diff。
4. 本目录当前不是 Git repository；不能依赖 `git status/diff` 判断变更。使用文件时间、测试、OpenAPI 生成和明确证据审计。

## 8. 建议的下一步顺序

1. 等待并验收 Figma 窗口当前 React 改造结果：测试、build、三档 viewport、控制台、横向溢出、缩略图请求。
2. 修复该窗口发现的任何 P0/P1 UI 回归；保持真实 API 与状态机不变。
3. 若 Figma MCP 额度恢复：先 inspect 文件，不重复 foundation；修复透明 wrapper，再建立 Buttons/Input/Select/Status/Nav/ModeCard/MediaCard 组件和 1440 生成工作台精修屏，逐 section 截图验收。
4. 将 Figma 的最终节点 ID、变量、组件、屏幕和 pending validation 回写 `figma-state.json`。
5. 回到严格门禁顺序：继续 G6 的非 Comfy 工作；只有用户明确说 ComfyUI 用完后，才恢复 H3 真样片、四条真实 proxy、崩溃隔离与恢复证据。
6. G6 PASS 后才进入 G7→G10 的正式验收与报告闭环。
7. 每阶段更新 migrations、OpenAPI、测试、缩略图/样片、恢复证据、退出报告、`requirements-traceability.md` 和风险登记。

## 9. 常用验证命令

在项目根目录执行：

```powershell
& '.\.venv\Scripts\python.exe' -m pytest apps/api/tests
pnpm web:test
pnpm web:build
```

不要使用系统默认 `python` 跑 pytest；该解释器没有项目 pytest。Playwright 使用项目已有配置和 Chromium 路径，产物放在 `output/playwright/`。任何给用户查看的截图先压缩为缩略图。

## 10. 完成判定

只有以下项目全部满足，才允许将目标标记完成：G0—G10 顺序门禁全部通过；86/86 FR；14/14 NFR；85 个主干测试；完整本地端到端 UAT；无 P0/P1；真实本地模型及异常恢复证据；release evidence；安装/升级/回滚手册；SBOM；已知限制；最终 go/no-go 报告。

> 2026-08-15 FR-VID-008 closure：新增 migration `0024_video_review_annotations`，审核页可为 VERIFIED 视频保存不可变时间码、问题分类、备注，以及可选的真实派生截图 MediaVersion 与同项目返工 Job 关联；服务端按真实 duration 限制时间码，并拒绝跨项目/非派生截图/非视频/未核验媒体。顺带修复普通本地视频 import 与 derive 未持久化 duration/fps 的既有缺陷。生产库在线备份、0023→0024 隔离升级/精确恢复演练及正式迁移均通过，integrity ok；三档生产只读 UAT 显示正式视频 4458ms 上限，零写入/溢出/短控件/浏览器错误/失败响应/公网/原片且未截图。全量 API 154 passed / 4 Comfy live deselected、Web 33/33、build/Ruff/mypy 94 files PASS。release audit 仍诚实为 IN_PROGRESS：G7 唯一模型许可证证据阻塞、G8/G9 仅 evidence PASS、最终工件非 FINAL。
